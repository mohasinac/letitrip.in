// Re-exported for backward compat — use RatingDisplay from '@/components' in new code
export { RatingDisplay as ReviewStars } from "@/components";
export type { RatingDisplayProps as ReviewStarsProps } from "@/components";
