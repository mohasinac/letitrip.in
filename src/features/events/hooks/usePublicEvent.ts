"use client";

// Delegate to @mohasinac/feat-events — single-event fetch
export { useEvent as usePublicEvent } from "@mohasinac/feat-events";
export type { EventItem as PublicEventResult } from "@mohasinac/feat-events";
