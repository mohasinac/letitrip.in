import { StoreCapabilitiesGuideView } from "@mohasinac/appkit";

export const metadata = {
  title: "Capabilities Guide | LetItRip Seller",
  description:
    "Per-store feature flags on LetItRip — what each capability unlocks, defaults, and how to request access.",
};

export default function Page() {
  return <StoreCapabilitiesGuideView />;
}
