import { AddStoreAddressClient } from "@/components/store/StoreAddressClients";

export const metadata = { title: "Add Pickup Address — Store" };

export default function Page() {
  return <AddStoreAddressClient />;
}
