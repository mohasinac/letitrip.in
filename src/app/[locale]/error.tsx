"use client";

/**
 * Per-Route Error Boundary — Handles runtime errors in route segments
 * @see https://nextjs.org/docs/app/api-reference/file-conventions/error
 *
 * Re-exports the centralized ErrorView component from appkit.
 */
export { ErrorView as default } from "@mohasinac/appkit/next";
