import type { AppkitConfig } from "@mohasinac/appkit";

// eslint-disable-next-line @typescript-eslint/no-require-imports
const appkitConfig: AppkitConfig = require("../../appkit.config.js");

export default appkitConfig;
