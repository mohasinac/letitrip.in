/**
 * Re-exports from @mohasinac/appkit — canonical source of truth.
 * Do not add constants here; add them to appkit/src/constants/field-names.ts
 * or to the relevant feature schema file in appkit/src/features/.
 */
export {
  // Products
  PRODUCT_FIELDS,
  PRODUCT_STATUS_TRANSITIONS,
  // Orders
  ORDER_FIELDS,
  // Reviews
  REVIEW_FIELDS,
  // Bids
  BID_FIELDS,
  // Ads
  AD_FIELDS,
  // Events (from appkit events feature)
  EVENT_FIELDS,
  EVENT_ENTRY_FIELDS,
  // Payouts (from appkit payments feature)
  PAYOUT_FIELDS,
  // Stores (from appkit stores feature)
  STORE_FIELDS,
  // Categories (from appkit categories feature)
  CATEGORY_FIELDS,
  // Blog
  BLOG_FIELDS,
  // Users (from appkit auth feature)
  USER_FIELDS,
  // Addresses (from appkit account feature)
  ADDRESS_FIELDS,
  // Brands
  BRAND_FIELDS,
  // Cart (from appkit cart feature)
  CART_FIELDS,
  // Wishlists / History
  WISHLIST_FIELDS,
  HISTORY_FIELDS,
  // Notifications (from appkit admin feature)
  NOTIFICATION_FIELDS,
  // Sessions (from appkit auth schemas)
  SESSION_FIELDS,
  // Coupon usage
  COUPON_USAGE_FIELDS,
  // Conversations
  CONVERSATION_FIELDS,
  // Scammers (from appkit scams feature)
  SCAMMER_FIELDS,
  // Support tickets (from appkit support feature)
  SUPPORT_TICKET_FIELDS,
  // Carousel
  CAROUSEL_FIELDS,
  // Coupons (from appkit promotions feature)
  COUPON_FIELDS,
  // FAQs
  FAQ_FIELDS,
  // Homepage sections
  HOMEPAGE_SECTION_FIELDS,
  // Site settings
  SITE_SETTINGS_FIELDS,
  // Common / shared
  COMMON_FIELDS,
  // Auth helpers
  OAUTH_STATE_VALUES,
  // Defaults
  SCHEMA_DEFAULTS,
} from "@mohasinac/appkit";
