/**
 * Next.js Middleware entry point.
 *
 * Next.js discovers this file automatically (src/middleware.ts).
 * All locale-routing logic lives in src/proxy.ts.
 */
export { default, config } from "./proxy";
