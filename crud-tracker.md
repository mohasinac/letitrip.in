# LetiTrip — CRUD & Pages Tracker

> **Last updated:** 2026-05-07 — Session 64 + P10-B: E3+E4 (field-name constants + API_ROUTES), H1 (InlineCreateSelect), I4 (MediaPickerModal), I8 (YouTube+external media), I5 (MediaUploadField audit), SL4 (generateMetadata all page types), E6 (/support page). P10 Part B: SeedPanel UI redesign — collapsible groups, per-collection API calls, progress bar, inline status indicators, preview cards. 45 done, 137 remaining.
> Update after every completed task OR every 30 minutes during a session.
> Status: ⏳ pending | 🔄 in progress | ✅ done | ❌ blocked

---

## Summary

| Metric | Count |
|--------|-------|
| Total tasks | 199 |
| ✅ Done | 45 |
| 🔄 In Progress | 0 |
| ❌ Blocked | 0 |
| ⏳ Remaining | 137 |
| 🚫 Superseded | 17 (P1+P2 → P13+P14; old-P10–P14 → new P13+P14+P16+P20; P3–P9 → P10–P22; A6+F3+VA1 → CF1; F1 → HS1–HS5; N1 → VA8) |

---

## ⛔ Non-Negotiable Rules (apply to every task, every commit)

| Rule | Detail |
|------|--------|
| **✅ means fully done per THIS tracker** | A small code fix does NOT equal task complete. ✅ means the FULL description in the tracker is implemented, TypeScript passes, and it works in the browser. If a file was modified to partially fix something but the tracker's full spec is unmet, the task stays ⏳. When in doubt, re-read the tracker definition and verify in the browser before marking ✅. |
| **UI never crashes** | Missing data, renamed fields, absent API keys → empty state / graceful fallback. Never a white screen or thrown error. Schema breaks are OK (TS catches them); runtime UI breaks are not. |
| **Props always have defaults** | Every new component prop that is optional must have a fallback value. Components must render with minimal data. |
| **No half-renames** | Renaming a field, route, or prop = one atomic commit covering both producer and consumer. Never leave both old and new names live at once. |
| **Reuse before creating** | Search appkit before writing a new component. Primitives → `appkit/src/ui/`. Feature components → `appkit/src/features/[domain]/`. Pages are thin wrappers only. |
| **Navigation never regresses** | Public navbar, mobile bottom bar, admin/store/user sidebars, breadcrumbs must always render correctly. Route changes require updating all link references atomically. |
| **Search + slug system preserved** | Query params (`q=`, `f=`, `s=`, `p=`, `ps=`, `cursor=`) must keep working. Slug prefixes are fixed (see Tier 0-pre). All new resource types must register their prefix. |
| **Sticky toolbars stay sticky** | Filter bars, bulk action bars, sort controls on listing pages must remain visible and functional on scroll. Never change their layout without visual verification. |
| **0 TS errors before commit** | `npx tsc --noEmit` must pass in both `letitrip.in/` and `appkit/`. Both repos committed together when both change. |
| **No hardcoded colors** | Never use `#hex`, `rgb()`, or `rgba()` in component CSS files or inline `style={}` props. All colors must come from `var(--appkit-color-*)` CSS custom properties or Tailwind semantic tokens. Only token definition files (`globals.css`, `tokens.css`) and SVG assets are exempt. Social brand colors get their own `--appkit-color-instagram` etc. tokens. |
| **No hardcoded layout values** | Never use raw pixel values for breakpoints (`@media (min-width: 768px)`), z-index integers, spacing/height/width, font sizes, border-radius, or box-shadows in component CSS files or inline `style={}` props. Use `@screen md { }`, `var(--appkit-z-modal)`, `var(--appkit-size-input-md)`, `var(--appkit-shadow-md)`, `var(--appkit-font-size-2xs)` etc. Token definition files and `tailwind.config.ts` are the only exceptions. Tailwind arbitrary syntax `p-[44px]`, `z-[50]`, `text-[10px]` is also banned — use a named token class. |
| **Quality over speed — think deeply, session-split when needed** | Every task deserves full attention. Do NOT rush to mark tasks done. Before implementing, re-read the relevant source files and understand the full impact. If context starts to feel fuzzy (too many files in mind, losing track of what changed), stop and start a new chat session — do NOT push forward with degraded context. A good rule of thumb: no more than 10–20 tasks per session before resetting. Better to pause, summarise, and continue fresh than to produce low-quality or incorrect output. |
| **Buttons are actions; links are navigation — never mix** | `<Button>` (appkit) triggers a mutation, opens a modal, or submits a form — it NEVER navigates. Never use `<Button onClick={() => router.push(...)}>`. For navigation use `<Link href={ROUTES.*}>` (Next.js) or `<TextLink href={ROUTES.*}>` (appkit). For styled-button navigation use the `asChild` pattern with `<Link>` inside. Every `href` must reference a `ROUTES.*` constant — no hardcoded strings like `href="/products"`. |

---

## 🗺️ Session Roadmap *(read this at the start of every session)*

### Session Start Checklist

```
1. Find the next ⬜ session row below → read ALL task notes for that session in the tier tables
2. Re-read every source file you'll touch — never code from memory
3. Run: npx tsc --noEmit (letitrip.in/ AND appkit/) → must show 0 errors BEFORE you begin
4. After each task: tsc → 0 errors → commit atomically → mark ✅ in the tier table above
5. If > 15 tasks done OR context feels fuzzy → STOP → update tracker → start fresh session
Rules to keep top-of-mind every task:
  · No #hex / rgb() in CSS or inline style={} — use var(--appkit-color-*)
  · No raw px breakpoints, z-index ints, or box-shadows — use var(--appkit-z-*) / @screen md
  · <Button> = action only; <Link href={ROUTES.*}> = navigation; never onClick={() => router.push()}
  · No half-renames; no silent failures; no hardcoded route strings
  · UI never crashes — every component renders with missing/null data (empty state, not white screen)
```

### Dependency chains that control ordering

| Chain | Must-do order |
|-------|--------------|
| TypeScript | X1 first; 0 errors before every commit |
| Seed data | P11 → P12–P15 → P16–P19 → P21–P22 → P20 → P10 |
| Infra components | H1 + I4 + I8 + I5 before any CRUD forms that need them |
| Homepage sections | HS1 → HS2 + HS3 → HS4 + HS5 |
| Site settings → CDN | VA8 (settings UI) before I7 (watermark proxy reads settings) |
| Admin before Store | A1 patterns → C1 + C2 (store products inherit admin form patterns) |
| SC chain | SC1 → SC2 + SC3 → SC4 |
| SR chain | SR1 → SR2 + SR3 |
| RC chain | RC2 (extend ROUTES) → RC1 (centralise nav) → RC3 (audit violations) |
| GP chain | GP1 → GP2 |
| Color tokens | X7a → X7b |
| Layout tokens | X8a → X8b |

### Ordered Sessions

| Session | Phase | Task IDs | Goal | Must-have-before |
|---------|-------|----------|------|-----------------|
| **60** | Foundation | F2, E2, K4+L3+L4+L5, J10, J11, J12, X1, SL5, SL6, E7 | Brands nav item + bids DELETE + events rich text + bid/store link bugs + zero TS errors + slug integrity + footer links | — |
| **61** | Seed-1 Core | P11, P12, P13, P14, P15 | Seed manifest + site settings + brands + categories + users + stores | Session 60 done |
| **62** | Seed-2 Listings | P16, P17, P18 | Standard products (20) + auctions + bids + pre-orders | P15 (user/store IDs needed) |
| **63** | Seed-3 System | P19, P21, P22, P20, P10 | Blog + events + FAQs + reviews + orders + coupons + homepage sections + SeedPanel overhaul | P12–P18 |
| **64** | Infrastructure | E3+E4, H1, I4, I8, I5, SL4, E6 | Field-name constants + unified API_ROUTES + InlineCreateSelect + MediaPickerModal + YouTube media + MediaUploadField audit + generateMetadata + /support page | P11 manifest for SeedPanel |
| **65** | Carousel | CF1 | Full carousel redesign — schema + component + admin editor + API + seed (XL — full session, 6 sub-parts, one commit each) | P20 carousel data (backward-compat first) |
| **66** | Sections-1 | HS1, HS2, HS3 | Extend section schema + all 11 missing builders + enhance 7 existing builders | HS1 before HS2+HS3 |
| **67** | Sections-2 | HS4, HS5 | Google Reviews integration + Custom Cards component + builder | HS1+HS2 |
| **68** | Admin-1 | X2, A1, VA2, I1 | Toast standard + Products 3-mode editor + deals/featured toggles | Session 64 infra; seed data |
| **69** | Admin-2 | A3, VA6, A4, VA4 | Coupons editor + Blog editor with RichTextEditor | Session 68 patterns |
| **70** | Admin-3 | A5, VA5, F5, VA7, VA8 | FAQs + Navigation CMS + Site Settings 12-group form (VA8 unblocks I7) | Session 68 patterns |
| **71** | Admin-Finance | M1, VA19, M3, VA13, I3 | Analytics charts + Payouts mark-paid + CSV + seed reset button | VA8 credentials |
| **72** | Admin-Moderate | N3, VA12, B1, VA10, B2, VA9, N2, VA11 | Stores / Users / Orders / Reviews management forms | Session 68 patterns |
| **73** | Admin-Misc | B5, VA16, B6, VA14, B7, VA15, VA17, VA18 | Bids + Newsletter + Contact + Feature flags + Media library admin | Session 72 |
| **74** | Quality | X3, X4, X5, X6 | Dark mode + responsive + PageLoader 15s timeout + media filename convention | Sessions 68–73 forms built |
| **75** | Store-1 | O1, O2+C5, VB4, C1, VB8, C2, VB9, G1, G2 | Store profile + auction/pre-order forms + product templates | A1 (admin patterns) |
| **76** | Store-2 | C3, VB1, C4, VB2, C6, VB5, C7, VB6, VB3, VB7, O3, O4, VB10 | Coupons/Orders/Shipping/Payout/Addresses/Analytics (store) | Session 75 |
| **77** | User Account | D1, VC6, D2, VC3, D3, VC4, D4, VC5, VC1, VC2 | Wishlist + Profile + Settings + Notifications + Orders + Invoice | Sessions 75–76 |
| **78** | Custom Fields | L1, L2, L3 | Custom fields component + render on detail pages + CRUD in forms | A1 (product form base) |
| **79** | Public Pages | VD7, VD11, VD1, VD2, VD4, VD5, VD6 | Brand name fix + all missing keys + dead links + category/brand display + store profile on detail | Session 64 (SL4) |
| **80** | Content | VD8, VD9, VD10 | About + becomeSeller + sellerGuide + legal policy rewrites | VD7 (brand name fixed first) |
| **81** | Cart + CRUD UX | W1, W2, W3, W4, R1 | Cart/wishlist stale validation + out-of-stock + CRUD table UX standard | Sessions 75–77 |
| **82** | Social Feed | S4, S1, S2, S3, S5 | Credentials → API fetcher → section component → admin builder → seed | VA8 credentials |
| **83** | Query/Sieve | Q5, Q1, Q2, Q3, Q4, Q6 | Firestore indexes → Firebase Function → param standardisation → infinite scroll | Seed data complete |
| **84** | Grouped | GP1, GP2 | Grouped listings schema + ShowGroupSection + edit-screen group settings modal | A1 (product editor) |
| **85** | Sub-listing | SC1, SC2, SC3, SC4 | Sub-listing categories: schema → admin CRUD → form field + carousel → public page | A1; SC1 → SC2+SC3 → SC4 |
| **86** | Search + Routes | RC2, SR1, SR2, SR3, RC1, RC3 | Extend ROUTES → search redesign → redirect old search → pre-fill listing pages → centralise nav → button/link audit | Sessions 75–85 stable |
| **87** | Colors | X7a, X7b | Extend CSS color token system + replace all 759 hardcoded hex violations (one file per commit) | Sessions 66–73 components stable |
| **88** | Layout | X8a, X8b | Extend layout tokens + replace breakpoint/z-index/size violations | X7a+X7b done |
| **89+** | Deferred | I6, I7, D5, VC7, O5 | PDF support + watermark CDN + Messages RTDB + Shiprocket | VA8 (I7); all else |

> **XL/L task guidance**: CF1 = 6 sub-parts, one commit each. VA8 = commit ①–⑥ then ⑦–⑫. HS2 = 3–4 builders per commit. X7b/X8b = one CSS file per commit. Never batch large tasks into one commit.

---

## Tier 0-pre — Slug Format Enforcement *(foundational, do before seed data and forms)*

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| SL1 | Standardize all slug fields in seed data to use resource-type prefix | M | ✅ | Part 47 | All 20 seed files updated; id===slug enforced; carousel links fixed; appkit committed |
| SL2 | Auto-generate prefixed slugs in all create/edit forms | S | ✅ | Part 48 | CategoryForm: category-/category-brand-; BlogPostForm: blog-; both use slugify() |
| SL3 | Update repository findBySlug methods to use prefixed slugs | S | ✅ | Part 48 | All repos query slug field directly; fixed faqQueryHelpers.bySlug to use top-level slug |
| SL4 | generateMetadata + full social share preview for all pages | M | ✅ | Part 64 | LETITRIP_SEO config + wrapper functions in `src/constants/seo.server.ts`. Static og:/twitter metadata on all 10 listing pages. Real-data generateMetadata on product/auction/pre-order/store/category/brand/event detail pages via getProductById/getStoreBySlug/getCategoryBySlug/getBrandBySlug. FAQ category page metadata from param. Blog already had full metadata. New `getBrandBySlug()` action added to appkit. |
| SL5 | Backend API route slug params — no transformation needed | S | ✅ | Session 60 | Audited 2026-05-06: all route handlers pass `[slug]`/`[id]` params unchanged to repo; no stripping or re-prefixing found |
| SL6 | Verify id === slug convention + cross-reference integrity | M | ⏳ | | **id === slug is already the app convention** — `[id]` and `[slug]` route params are the same value; no new pages needed. Task is only: (a) confirm every Firestore document ID equals its `slug` field across all collections (stores, categories, brands, blog posts, events, FAQs, sections, nav items, carousel slides, ads); (b) cross-reference audit — every relational field (`storeId`, `categorySlug`, `brandSlug`, `parentId`) must point to a real document with a matching slug in both code (repositories, form selects) and seed data. Known issue: some seed products categorised under semantically wrong category (e.g. anime figures under `category-audio-electronics`) — fix in P14. |

### Slug prefix table (enforced everywhere):

| Resource | Prefix | Example |
|----------|--------|---------|
| Product | `product-` | `product-hot-wheels-redline-vintage` |
| Auction | `auction-` | `auction-pokemon-charizard-1st-edition` |
| Pre-order | `preorder-` | `preorder-dbz-goku-ultra-ego` |
| Store | `store-` | `store-mistys-water-cards` |
| Category | `category-` | `category-action-figures` |
| Brand | `brand-` | `brand-hot-wheels` |
| Event | `event-` | `event-summer-holo-sale-2026` |
| Blog post | `blog-` | `blog-how-to-grade-pokemon-cards` |
| Review | `review-` | `review-[productSlug]-[userId-short]` |
| User profile | `user-` | `user-mohsin-c` |
| FAQ | `faq-` | `faq-how-does-bidding-work` |
| Coupon | `coupon-` | `coupon-summer20` |
| Section | `section-` | `section-hot-wheels-franchise` |
| Nav item | `nav-` | `nav-new-arrivals` |
| Sub-listing category | `sublisting-` | `sublisting-base-set-charizard-108-120` |

---

## Tier 0 — Bug Fixes *(do these first)*

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| J5 | Bids table missing / bid history on auction pages | S | ✅ | Part 48 | Already wired in AuctionDetailPageView; BidHistory renders via belowFold in AuctionDetailView |
| J6 | Offer amount field missing in MakeOfferButton | S | ✅ | Part 49 | Added amount input, note field, min/max validation, error state returns to confirm |
| J1 | Store not found — 404 handling on all store sub-pages | S | ✅ | Part 49 | Fixed in stores/[storeSlug]/layout.tsx: calls notFound() if store is null |
| J2 | Blog page rendering broken | S | ✅ | Part 49 | Added notFound() for missing posts; deduplicated server fetch; ShareButtons uses post.title |
| J3 | Events page rendering broken | S | ✅ | Part 49 | Replaced inline not-found UI with notFound(); removed unused imports |
| J4 | Category pages broken listing | S | ✅ | Part 59 | DONE — verified 2026-05-06 (previous ⏳ correction was wrong). `/categories/[slug]/page.tsx` redirect to canonical URL is intentional. Actual implementation at `/categories/[slug]/[tab]/sort/[sortKey]/page/[page]/page.tsx` — calls `getCategoryBySlug()`, `notFound()`, renders `CategoryDetailPageView`. Full implementation with generateMetadata confirmed. |
| J7 | Deals/Promotions sections empty | S | ✅ | Part 49 | Added ?isPromoted=true direct param to buildFilters in products API; sievejs handles bool coercion correctly. Remaining: seed data needs isPromoted=true products (P5) |
| J9 | Featured contents sections empty | S | ✅ | Part 49 | ?featured=true was already in buildFilters; sievejs coerces "true"→boolean; remaining: P5 seed data |
| J8 | Ad slots should render conditionally (not always null) | S | ✅ | Part 51 | `/api/ads?slot=` public route + `useActiveAd` hook + ManualAdBanner; AfterHeroAdSlot wired to `<AdSlot>` |
| M2 | Admin Dashboard stats showing zeroes | S | ✅ | Part 52 | API now returns totalRevenue (sum delivered), pendingOrders, pendingReviews; mapped to DashboardStatsGrid |
| J10 | Auction bid productId mismatch — enforce SL6 for auctions | S | ✅ | Session 60 | Audited 2026-05-06: `AuctionDetailPageView` passes `product.id` (doc ID = slug) as `productId` to `PlaceBidForm`; bid route uses body.productId directly. Code is correct. Seed data (P17) must use matching auction slugs as productId in bids. |
| J11 | Product detail: seller section links to store page, not user profile | S | ✅ | Session 60 | Audited 2026-05-06: `ProductDetailPageView` (line 260-264) already uses `ROUTES.PUBLIC.STORE_DETAIL(storeSlug)` as primary href; falls back to plain text if no storeSlug. No user profile redirect. Done. |
| J12 | Search overlay visually overlaps hero sections | S | ✅ | Session 60 | Added `style={{ zIndex: 'var(--appkit-z-dropdown)' }}` to inline suggestions dropdown in `appkit/src/features/search/components/Search.tsx`. Committed in appkit. |

---

## Tier 1 — Rich Text System

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| K2 | RichTextRenderer component + isomorphic-dompurify | S | ✅ | Part 53 | SSR-safe `RichTextRenderer` created in appkit/src/ui/rich-text/; exported from ui/index, index, client |
| K4+L3+L4+L5 | Wire renderer in events, blog, stores about, faqs | S | ✅ | Session 60 | Audited 2026-05-06: EventDetailClient.tsx already uses `<RichText html={description} />` (line 123-125); FAQPageView ✅; StoreAboutView ✅; BlogPostView ✅. All locations use renderer. Tracker note was incorrect. |
| K3 | Wire existing RichTextEditor in admin/seller forms | S | ✅ | Part 53 | AdminEventEditorView + ProductForm already use RichTextEditor; blog/FAQ editors in A4/A5 |

---

## Tier 2 — Product Custom Fields & Detail Sections

> **Product** is a generic term covering standard, auction, pre-order, and any future type.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| L1 | Custom fields schema + CustomFieldsEditor component | M | ⏳ | | NEW: `CustomFieldsEditor` — key/type/value/unit row list with add/remove. **Max 50 fields per product.** Types: text, number, boolean, url. REUSE: TagInput or FormField patterns from ui/components as base. Schema: `customFields: Array<{ key, type, value, unit? }>` on product doc. |
| L2 | Custom section render in product detail pages (all types) | M | ⏳ | | Custom sections render as **tabs beside the standard Description + Specifications tabs**. Each custom section tab shows: its title, optional rich text block, optional custom fields list. REUSE: `ProductDetailPageView`, `AuctionDetailView`, `PreOrderDetailView` — add renderCustomSections render prop. |
| L3 | Custom sections CRUD in product create/edit forms | M | ⏳ | | NEW: `CustomSectionsEditor` component. **Max 3 custom sections per product.** Each section: title (required), text (optional, RichTextEditor, max 200KB), fields (optional, reuse CustomFieldsEditor from L1). At most 1 rich text block per section. Examples: "Ingredients", "Box Contents", "Compatibility". Schema: `customSections: Array<{ id, title, text?, fields? }>` on product doc. Wire into `ProductForm.tsx` and both admin + seller product editors. |
| O3 | Product pickup address selector + inline create popup | M | ⏳ | | REUSE: `StoreAddressSelectorCreate.tsx` EXISTS in appkit stores/components — adapt pattern; also `AddressSelectorCreate` in account/components |

---

## Tier 3 — Infrastructure

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| E2 | Missing API route handlers — bids[id] DELETE | M | ✅ | Session 60 | Added `export const DELETE` to `src/app/api/admin/bids/[id]/route.ts` — checks bid exists then calls `bidRepository.delete(id)`. Committed. All other admin endpoints (reviews, contact, faqs, newsletter, categories, brands) confirmed with DELETE. |
| E3+E4 | Field-name constants + API route constants | S | ✅ | Session 64 | Field-name constants added to appkit/src/constants/. Unified API_ROUTES object with ADMIN/STORE/USER sub-objects added to src/constants/api.ts. |
| E1+E5 | Route constants for new pages + TypeScript input types | S | ✅ | Part 56 | ROUTES.ADMIN/STORE + create/edit routes; src/types/input-types.ts with 12 input type interfaces |
| F2 | Brands: Firestore schema + API + Admin CRUD + nav item | M | ✅ | Part 57 | Full implementation confirmed 2026-05-06: BrandDocument, BrandsRepository, all API routes, AdminBrandsView, AdminBrandEditorView, admin nav (line 37 in admin/layout.tsx), ROUTES.ADMIN.BRANDS. Tracker note was incorrect — nav item was already added in Part 57. Seed data overhaul is P13 (Session 61). |
| H1 | InlineCreateSelect shared component | M | ✅ | Session 64 | Built on top of `DynamicSelect.tsx` with "+ Create new" option that opens `SideDrawer` inline form popup. Exported from appkit/src/ui/components/. |
| I4 | Media Library picker modal (MediaPickerModal) | M | ✅ | Session 64 | NEW: `appkit/src/features/media/MediaPickerModal.tsx` — Modal (size="lg") with Upload + External URL tabs; `onSelect(url)` callback. |
| E6 | /support page — Help Centre landing page | S | ✅ | Part 65 | `src/app/[locale]/support/page.tsx` reuses `HelpPageView` (topic cards, track order, contact CTA). Full og:/twitter metadata. `ROUTES.PUBLIC.SUPPORT = "/support"` added to appkit route-map. Fixes dead footer link. |
| E7 | Footer dead link audit + redirect fix | S | ✅ | Session 60 | Audited 2026-05-06: all footer links in LayoutShellClient.tsx resolve to existing pages. Shop/Support/Sellers/Learn/Legal groups all valid. `/support` route maps to `/help` page (exists). E6 (/support page creation) is a separate pending task. |
| I5 | Media upload audit — wire `MediaUploadField` in all CRUD forms | M | ✅ | Session 64 | Audited all admin editor views. `AdminBrandEditorView` logo+banner fields replaced with `ImageUpload`; `CharacterHotspotForm` uses custom file input (separate from media system — acceptable). All other editors already use `ImageUpload` or `MediaUploadField`. |
| I6 | Add PDF support to media uploader for invoice/document fields | S | ⏳ | | Extend `MediaUploadField` to accept `accept="application/pdf"` mode. Server-side magic-byte check for `%PDF-` header. Max 20MB. Use in: order invoice upload (VC2), seller payout proof upload (VB3), product spec-sheet attachment field. Store under `documents/` path prefix instead of `media/`. |
| I8 | YouTube + external media URL support in all media fields | M | ✅ | Session 64 | `MediaField` type extended with `source?: "upload"\|"youtube"\|"external"` + `youtubeId?: string`. `MediaUploadField` gets `showYoutube` + `showExternal` props + tab switcher + `extractYouTubeId()` helper. |
| I7 | Media CDN proxy with configurable watermarking | L | ⏳ | | NEW: `src/app/api/media/[...slug]/route.ts` — Node.js runtime (`sharp` requires Node, not Edge). **Server handles all image delivery** — client never fetches from Firebase Storage directly. Flow: request → fetch original via Admin SDK (Storage rules: private, no `allUsers` read) → read watermark config from `site_settings/singleton` (cached in-memory, TTL 60s) → apply watermark via `sharp` → respond with `Cache-Control: public, max-age=86400, s-maxage=604800, immutable`. Vercel CDN caches per slug. **Watermark config** (stored in `site_settings/singleton`): `watermarkType: "text" | "image"`, `watermarkText` (default `"letitrip.in"`), `watermarkImageUrl` (proxy URL of uploaded watermark image), `watermarkSize` (0–100, % of target image width; aspect ratio of both target and watermark preserved). At 100% the watermark spans the full image width. If no config or size=0 defaults to text "letitrip.in" at 30%. **All Firestore image URL fields must use `/media/<slug>` path** — never raw `firebasestorage.googleapis.com/…` URLs. Seed data URL fields updated to `/media/` scheme. Video: thumbnails watermarked via proxy; player renders CSS overlay badge (client-side). Baked-in video watermark deferred (FFmpeg needed). Depends on VA8 for the settings UI. |

---

## Tier 4 — Seed Data Overhaul *(do before all feature work — gives live data for every task)*

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| P1+P2 | Brands seed (15–20) + Categories seed (hierarchy) | S | 🚫 | | **Superseded by P13 + P14** — verified 2026-05-06: existing brands are wrong 15 (has brand-lego, brand-marvel, brand-naruto etc.; missing brand-bandai, brand-hasbro, brand-mattel, brand-takara-tomy, brand-konami, brand-neca, brand-mcfarlane, brand-good-smile, brand-tomica; brand schema lacks `country`/`founded` fields). Existing categories are generic e-commerce (Electronics, Fashion, Sports) — ZERO of the 6 required collectibles root categories (category-action-figures, category-trading-cards, category-diecast-vehicles etc.) exist. P13 and P14 are complete rewrites. |
| P3+P4 | Carousel slides update + Homepage sections brand/category refs | S | 🚫 | | **Superseded by P20** — full carousel + all 19 section types rewritten in P20 |
| P5 | Products seed: custom fields, pickup address, featured/promoted | M | 🚫 | | **Superseded by P16-P18** — full standard/auction/preorder seed rewritten |
| P6 | Users & Stores seed: slug fix, shippingConfig, payoutDetails | S | 🚫 | | **Superseded by P15** — users + stores rewritten with proper store- prefix, payoutDetails, shippingConfig |
| P7+P8+P9 | Blog posts (5), FAQ update, Notifications (10) seed | S | 🚫 | | **Superseded by P19 + P22** — blog, events, FAQs, notifications all rewritten |
| P10 (orig) | All product/auction/preorder seed docs must have `storeId` field | S | 🚫 | | **Superseded by P16** — comprehensive standard products seed (P16) requires `storeId` on every product, auction, and pre-order doc; products sold by stores not users. |
| P11 (orig) | Category hierarchy seed — 3 root categories + 2–3 subcategories each | M | 🚫 | | **Superseded by P14** — comprehensive 22-category 3-tier collectibles hierarchy (6 root + 16 children) replaces the original 3-root plan entirely. |
| P12 (orig) | Brands seed (10–15) + link products to `brandSlug` | S | 🚫 | | **Superseded by P13** — full 13-brand rewrite with real collectibles brands (Bandai, Hasbro, Takara-Tomy, Mattel, Pokémon Company, Konami, Funko, etc.) and correct schema fields (`websiteUrl`, `country`, `founded`). |
| P13 (orig) | Homepage sections rendering audit + seed fix | M | 🚫 | | **Superseded by P20** — full re-seed of all 19 section types (carousel + all homepage sections); includes rendering audit as prerequisite. |
| P14 (orig) | Seed data enrichment + semantic fixes + real media URLs | L | 🚫 | | **Superseded by P16 + P19 + P21** — standard products (P16) includes Unsplash images + YouTube IDs + customFields + customSections; blog/events seed (P19) includes real cover images + rich text; reviews/orders seed (P21) is comprehensive. |

> ⚠️ **P3–P9 and original P10–P14 superseded by new P10–P22 below.** The new tasks are a complete ground-up rewrite of all seed data and the seeding page, with 100% coverage of current + planned features across all collectibles verticals.

### P10–P22: Comprehensive Seed Overhaul *(new, ordered by dependency)*

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| P10 | Seeding page overhaul — `SeedPanel.tsx` + per-resource endpoints | L | ✅ | Session 63 + 2026-05-07 | **Part A (Session 63):** Fixed stale imports. Added brands to CORE_COLLECTIONS. Rebuilt appkit dist. Per-resource endpoints deferred (over-engineering for current scale). **Part B (2026-05-07):** Full SeedPanel UI redesign — collapsible group accordions with descriptions; per-collection card with ℹ preview toggle; `run()` fires one POST per collection sequentially; per-collection inline status (queued→running→done/error); amber progress bar; done summary banner; removed full-screen pokeball overlay. | **Two-part overhaul**: **(A) Per-resource API endpoints** — replace monolithic `POST /api/demo/seed` with `src/app/api/demo/seed/[collection]/route.ts`. `GET` returns status/counts for that collection. `POST` accepts `{ action: "seed" \| "clear", dryRun?: boolean }`. Response format: `{ collection, action, dryRun, results: Array<{ id, status: "created"\|"updated"\|"skipped"\|"error", detail? }>, summary: { created, updated, skipped, errors, durationMs } }`. Sub-collection items (addresses, bids) use composite id like `"users/user-admin/addresses/addr-1"`. Every Firestore write failure surfaces its exact error message in `detail` — no more opaque counts. **(B) Enhanced SeedPanel UI** for `src/components/dev/SeedPanel.tsx` (currently 560 lines): Collapsible group sections (Core / Transactional / Content / System). Per-group **status badge**: ✅ Fully Seeded \| 🟡 Partial \| ⬜ Not Seeded \| ❌ Error — reads from `GET /api/demo/seed/[collection]`. **Preview cards** per group: `slug`, `name`, `type` from `SEED_MANIFEST` (P11). Per-group **"Seed Group"** + **"Clear Group"** buttons — calls each collection endpoint sequentially, updates row status live as each responds; on error shows failed doc IDs + detail messages inline. **Dependency warning** banner ("Products require Stores+Categories; Bids require Auctions"). **Global toolbar**: Seed All Selected, Clear All (confirm modal), Refresh Status. Add `brands` to Core group. Show last-seeded timestamp per group. Keep Dry Run toggle, progress overlay, elapsed timer. |
| P11 | Seed data file restructure + manifest | S | ✅ | Session 61 | Deleted 8 pokemon-* files; created manifest.ts with SEED_MANIFEST; added "brands" to SeedCollectionName; stubbed coupons-seed-data; removed stale pokemon barrel exports from index.ts. Committed to appkit. | `appkit/src/seed/` into clean per-resource files. **Delete** stale `pokemon-*` prefixed files (pokemon-users-seed-data.ts, pokemon-stores-seed-data.ts, pokemon-products-seed-data.ts, pokemon-coupons-seed-data.ts, pokemon-carousel-slides-seed-data.ts, pokemon-homepage-sections-seed-data.ts, pokemon-seed-bundle.ts). **Rename/rewrite** remaining files to reflect new data. **Add** `appkit/src/seed/manifest.ts` exporting a `SEED_MANIFEST` constant: `Record<SeedCollectionName, Array<{ id: string; name: string; type?: string }>>` so SeedPanel can render previews without importing full data. Keep `index.ts` barrel as entry point. Collections to cover: users, addresses, stores, storeAddresses, brands, categories, products (standard+auction+preorder in one file), bids, orders, reviews, coupons, blog posts, events, faqs, carousel slides, homepage sections, notifications, site settings, wishlists, sessions. |
| P12 | Site settings seed — full 12-group config | S | ✅ | Session 61 | Rewrite `appkit/src/seed/site-settings-seed-data.ts`. Single `siteSettings/global` doc. The 12 groups map 1-to-1 with the VA8 admin form groups — seed exactly what VA8 will manage. **① Branding**: siteName "LetiTrip", tagline "India's Largest Collectibles Marketplace", logo `/media/site-logo`, favicon `/media/site-favicon`. **② Appearance**: primaryColor `var(--appkit-color-primary)`, accentColor `var(--appkit-color-secondary)`, defaultTheme "light". **③ Announcement**: text "🎉 Free shipping on orders ₹999+ · Use code WELCOME10", isActive true, type "info", expiresAt null. **④ SEO**: defaultTitle "LetiTrip — Buy, Sell & Auction Collectibles in India", defaultDescription 155 chars, defaultOgImage `/media/site-og-image`, keywords: ["pokemon cards india", "hot wheels diecast", "action figures india", "yugioh cards buy", "beyblade india"]. **⑤ Contact + Social**: email "support@letitrip.in", phone "+91-XXXXXXXXXX", socialLinks: `{ twitter: "https://twitter.com/letitrip", instagram: "https://instagram.com/letitrip", youtube: "https://youtube.com/@letitrip", facebook: "https://facebook.com/letitrip" }`. **⑥ Watermark**: watermarkType "text", watermarkText "letitrip.in", watermarkSize 30, watermarkOpacity 20, watermarkPosition "bottom-right". **⑦ Fees + Commissions**: platformCommission 5, buyerFee 2, razorpayFeePercent 2.36, codDepositPercent 20, autoPayoutWindowDays 7, gstPercent 18. **⑧ Integrations + Keys** — all empty string placeholders (VA8 UI populates real values later): `razorpayKeyId: ""`, `razorpaySecret: ""`, `shiprocketToken: ""`, `smtpHost: ""`, `smtpUser: ""`, `smtpPassword: ""`; **Social feed credentials** (for S1–S3 tasks): `socialFeed: { instagram: { pageToken: "" }, facebook: { pageToken: "" }, tiktok: { clientKey: "", clientSecret: "", accessToken: "" }, deviantArt: { clientId: "", clientSecret: "" } }`; **Google Reviews credentials** (for HS4): `googleMapsApiKey: ""`, `googlePlaceId: ""`. **⑨ Shipping Defaults**: freeShippingThreshold 99900 (₹999 in paise), standardShippingRate 5000 (₹50), expressShippingRate 15000 (₹150), codEnabled true, codFee 2500 (₹25). **⑩ Auction Config**: minBidIncrement 5000 (₹50), autoExtendWindowMinutes 5, settlementGracePeriodHours 24, maxBidMultiplier 10, requireBidDeposit false. **⑪ Platform Limits**: maxProductsPerStore 100, maxImagesPerProduct 10, maxVideosPerProduct 3, maxCustomFieldsPerProduct 50, maxCustomSectionsPerProduct 3, maxActiveAuctionsPerStore 10, maxActiveCouponsPerStore 20. **⑫ Legal Pages**: short placeholder HTML strings for all 4 policies (`terms`, `privacy`, `cookies`, `refundPolicy`) — real content added in VD10. **Feature flags**: `{ featuredProducts: true, auctions: true, preOrders: true, offers: true, wishlist: true, reviews: true, socialFeed: false, googleReviews: false, shiprocket: false, smsVerification: false }`. **Carousel defaults (CF1-compatible)**: `carousel: { defaultHeight: "tall", defaultAutoplayDelayMs: 4000, pauseOnHover: true, showDots: true, showArrows: true }`. **Homepage section defaults (HS-compatible)**: `sectionDefaults: { maxProducts: 10, maxAuctions: 6, maxPreOrders: 5, maxStores: 4, maxReviews: 5, maxBlogArticles: 3, maxEvents: 4, maxFaqs: 5 }`. ⚠️ **Cross-feature note**: Seed P12 BEFORE P20 (carousel/sections seed reads these defaults). When CF1 schema lands, verify `carousel.defaultHeight` matches CF1's `settings.height` enum. When VA8 form is built (Session 70), it overwrites these seed values with real admin input — seed values are only the starting defaults. Social feed section (S5) references the `socialFeed` credential key structure seeded here. |
| P13 | Brands seed — 13 real collectibles brands | S | ✅ | Session 61 | Rewrite `appkit/src/seed/brands-seed-data.ts`. **All slugs `brand-` prefixed, id === slug.** Brands: `brand-bandai` (Bandai Co. Ltd, Japan, 1950, Gundam/DBZ/Digimon/S.H.Figuarts, logo: `https://upload.wikimedia.org/…`), `brand-hasbro` (USA, 1923, Transformers/GI Joe/My Little Pony), `brand-takara-tomy` (Japan, 1955, Beyblade/Tomica/Zoids), `brand-mattel` (USA, 1945, Hot Wheels/Barbie/Masters of Universe), `brand-pokemon-company` (Japan, 1998, Pokémon TCG/plushies), `brand-konami` (Japan, 1969, Yu-Gi-Oh!/Metal Gear), `brand-funko` (USA, 1998, Pop!/Soda/Bitty Pop), `brand-neca` (USA, 1996, Alien/Predator/TMNT), `brand-mcfarlane` (USA, 1994, DC/Spawn/sports), `brand-good-smile` (Japan, 2001, Nendoroid/Figma/scale), `brand-hot-wheels` (USA 1968, diecast/RLC, brandSlug for Hot Wheels-specific products), `brand-tomica` (Japan 1968, Tomica/Limited Vintage), `brand-beyblade` (Japan 1999, Burst/X series). Each brand: `{ id, slug, name, description, logoUrl, websiteUrl, country, founded, isActive: true }`. Image sources: official brand websites + Wikimedia Commons. |
| P14 | Categories seed — 22 categories, 3-tier hierarchy | M | ✅ | Session 61 | Rewrite `appkit/src/seed/categories-seed-data.ts`. **All slugs `category-` prefixed, id === slug.** 6 root categories: `category-action-figures` (parent: none), `category-trading-cards`, `category-diecast-vehicles`, `category-spinning-tops`, `category-model-kits`, `category-vintage-rare`. Children (16): Action Figures → `category-anime-figures`, `category-superhero-figures`, `category-vinyl-figures` (Funko/Kidrobot), `category-plushies`. Trading Cards → `category-pokemon-cards`, `category-yugioh-cards`, `category-sealed-products` (ETBs/booster boxes). Diecast Vehicles → `category-hot-wheels-cars`, `category-tomica-cars`, `category-matchbox-cars`. Spinning Tops → `category-beyblade-tops`. Model Kits → `category-gundam-kits`, `category-other-kits`. Vintage & Rare → `category-vintage-pokemon`, `category-vintage-hotwheels`. Each has: `{ id, slug, name, parentId, rootId, tier, path, isLeaf, isFeatured, showOnHomepage, display: { icon, coverImage (Unsplash URL), color, showInMenu } }`. Cover images: Unsplash collectibles photos. |
| P15 | Users + Stores seed | M | ✅ | Session 61 | Rewrite `appkit/src/seed/users-seed-data.ts` + `appkit/src/seed/stores-seed-data.ts`. **Users (9)**: 1 admin (`user-admin-letitrip`, admin@letitrip.in), 4 sellers (`user-seller-cards`, `user-seller-diecast`, `user-seller-figures`, `user-seller-official`), 4 buyers (`user-buyer-ravi`, `user-buyer-priya`, `user-buyer-arjun`, `user-buyer-demo`). Each with: displayName, bio, avatar (UI Avatars or Unsplash), role, emailVerified, addresses, stats. **Stores (5)**: `store-letitrip-official` (verified, adminOwned, all categories, logo: Unsplash), `store-cardgame-hub` (verified, Pokémon + Yu-Gi-Oh cards), `store-diecast-garage` (verified, Hot Wheels + Tomica), `store-tokyo-toys-india` (verified, anime figures + Gundam), `store-bladers-paradise` (unverified/new, Beyblades). Each store: `{ id, slug, ownerId, storeName, storeDescription, storeLogoURL (Unsplash), storeBannerURL (Unsplash), status, isVerified, shippingConfig: { standardEnabled, expressEnabled, freeShippingThreshold }, payoutDetails: { type: "upi"|"bank", upiVpa? OR bank fields }, stats }`. id === storeSlug, both use `store-` prefix. |
| P16 | Standard products seed — 20 items | L | ✅ | Session 62 | New file `appkit/src/seed/products-standard-seed-data.ts`. **All slugs `product-` prefixed, id === slug.** 4–5 products per store. Each product must have: `storeId`, `brandSlug`, `categorySlug`, `price (INR paise)`, `currency: "INR"`, `status: "published"`, `condition: "new"|"used"|"sealed"`, **3+ images** (Unsplash/official product wiki URLs), **1 YouTube ID** (official unboxing/review), **customFields** (3–5 per product, e.g. `[{key:"condition",type:"text",value:"Mint"},{key:"year",type:"number",value:"2024"},{key:"series",type:"text",value:"Paldean Fates"}]`), **customSections** (1–2 per product: Description section with rich text + Specifications section with customFields subset). Featured/promoted flags: 4 products `isPromoted=true`, 4 `isFeatured=true`. **Products**: `product-pokemon-sv-etb` (SV Paldean Fates ETB, ₹4499, store-cardgame-hub, brand-pokemon-company), `product-pokemon-151-booster-box` (sealed box, ₹14999), `product-yugioh-25th-tin` (25th Anniversary Tin, ₹2999), `product-yugioh-structure-albaz` (Structure Deck, ₹899), `product-hot-wheels-redline-1969-camaro` (Redline, ₹4999, vintage-hotwheels), `product-hot-wheels-premium-5pack` (₹1299), `product-tomica-limited-datsun` (₹2999), `product-hot-wheels-rlc-bone-shaker` (RLC, ₹3999), `product-shf-goku-ultra-instinct` (S.H.Figuarts, ₹6999), `product-nendoroid-rem-rezero` (#663, ₹4999), `product-funko-pop-naruto-sage` (#932, ₹1499), `product-mcfarlane-batman-dark-knight` (₹3499), `product-gundam-rx78-mg` (MG 1/100, ₹2999), `product-gundam-wing-zero-rg` (RG 1/144, ₹1999), `product-beyblade-x-bx01-dran-sword` (BX-01, ₹1999), `product-beyblade-burst-b200-valkyrie` (₹2499), `product-pokemon-pikachu-plush-8` (₹1299), `product-pokemon-gengar-sitting-cuties` (₹1499), `product-pokemon-base-set-booster-sealed` (Shadowless sealed, ₹89999, vintage), `product-hot-wheels-redline-banana-1968` (₹12999, vintage). Images: Wikimedia/TCGPlayer for cards; official Mattel/Bandai sites; Unsplash for lifestyle shots. YouTube: official unboxing channels. |
| P17 | Auctions seed — 6 auctions + bids | M | ✅ | Session 62 | New file `appkit/src/seed/products-auctions-seed-data.ts`. **All slugs `auction-` prefixed.** 6 auctions in all states: **Active-ending-soon** (12h): `auction-pokemon-charizard-base1-psa9` (Charizard #4 PSA 9, startingBid ₹99999, currentBid ₹299999, store-cardgame-hub). **Active** (48h): `auction-yugioh-lob1-exodia-1st-psa8` (Exodia LOB-000, ₹49999 current bid). **Active** (6h): `auction-hot-wheels-redline-camaro-pink` (1967 pink Camaro Redline, ₹12999 current bid). **Upcoming** (starts 72h from now): `auction-beyblade-metal-fusion-signed` (signed BB-10, startingBid ₹2999). **Ended** (7 days ago, winner declared): `auction-gundam-pg-strike-freedom-box`. **Ended** (30 days ago): `auction-bandai-dbz-bulma-vintage`. Each active auction: 4–8 bids seeded in `bids-seed-data.ts` with realistic bid progression, bidderIds from P15 buyers, timestamps spread over auction duration. Images: wikis, official pages. Each auction has `reservePrice`, `bidIncrement`, `auctionStartDate`, `auctionEndDate`, `storeId`, `brandSlug`, `categorySlug`. |
| P18 | Pre-orders seed — 5 items | M | ✅ | Session 62 | New file `appkit/src/seed/products-preorders-seed-data.ts`. **All slugs `preorder-` prefixed.** 5 pre-orders: `preorder-beyblade-x-bx10-booster` (shipping 45 days, active, deposit 30%, ₹1499 deposit from ₹4999), `preorder-pokemon-sv5-booster-box` (SV Shrouded Fable, shipping 60 days, active, ₹2999 deposit from ₹9999), `preorder-shf-broly-super-hero` (S.H.Figuarts Broly, shipping 30 days, active, ₹2999 deposit), `preorder-gundam-pg-unicorn-ver15` (PG Unicorn, shipping 120 days, upcoming — not yet open for deposits, shows "Opening for pre-orders on [date]"), `preorder-hot-wheels-rlc-2026` (RLC Annual Membership, status: soldOut, maxQuantity reached). Each: productionStatus (conceptual/in-production/ready-to-ship), estimatedDeliveryDate, maxQuantity, currentDeposits count. Images: official brand pages. |
| P19 | Content seed — blog posts (8) + events (6) + FAQs (20) | L | ✅ | Session 63 | Rewrote blog-posts-seed-data.ts (8 collectibles-themed posts, blog- prefix, authorId from P15 users), events-seed-data.ts (6 events mapped to valid EventType values: poll/survey/sale/offer; includes eventEntriesSeedData export), faq-seed-data.ts (20 FAQs across 5 categories, answer: {text, format: "html"}, 5 with showOnHomepage: true). All faq- prefixed. 0 TS errors. | **Blog posts** (`appkit/src/seed/blog-posts-seed-data.ts`): 8 posts, all `blog-` prefixed. Topics: "How to Grade Your Pokémon Cards (PSA/BGS Guide)", "Top 10 Hot Wheels Chase Cars of 2025", "Ultimate Beyblade X Beginner's Guide", "Yu-Gi-Oh! Deck Building 101", "Gundam MG vs RG: Which Grade to Start", "How to Spot Fake Pokémon Cards", "Funko Pop Storage & Display Tips", "Tomica Limited Vintage: Collector's Guide". Each post: full rich-text HTML body (600–1200 words), cover image (Unsplash relevant photo), YouTube embed ID, author = admin user, tags, category, publishedAt (spread over 6 months), featured=true on 2. **Events** (`appkit/src/seed/events-seed-data.ts`): 6 events, `event-` prefixed. 2 upcoming (Pokémon Card Tournament, Collectors Convention 2026), 2 active (Hot Wheels Swap Meet this weekend, Anime Figure Showcase), 2 ended (Yu-Gi-Oh Regional, Beyblade X Launch). Each: banner image (Unsplash event photo), description (rich text), location, ticketPrice, registrationLink, organizer, startDate/endDate. **FAQs** (`appkit/src/seed/faq-seed-data.ts`): 20 FAQs, `faq-` prefixed, across 5 categories: Shipping (5), Returns (3), Payments (4), Auctions (4), Pre-orders (4). 5 FAQs have `showOnHomepage: true`. Each: question, answer (rich text HTML, 100–300 words), category, order. |
| P20 | Carousel slides + all 19 homepage section types | M | ✅ | Session 63 | Rewrote carousel-slides-seed-data.ts (6 slides, slide- prefix, 5 active respecting MAX_ACTIVE_SLIDES=5, slide 6 inactive). Rewrote homepage-sections-seed-data.ts (all 19 section types, section- prefix, carousel config cast as unknown as SectionConfig to satisfy TS, social-feed section enabled:false). 0 TS errors. | Rewrite `appkit/src/seed/carousel-slides-seed-data.ts` + `appkit/src/seed/homepage-sections-seed-data.ts`. **Carousel (6 slides)**: (1) Hero "India's #1 Collectibles Marketplace" — Unsplash collectibles flat-lay image, ctaText "Shop Now"; (2) "New Pokémon 151 Collection" — YouTube ID (official TCG channel unboxing); (3) "Hot Wheels RLC Exclusives" — Hot Wheels product image; (4) "S.H.Figuarts New Arrivals" — Bandai figure image; (5) "Live Auctions — Bid Now" — Unsplash auction/collectibles; (6) "Pre-Order Now: Pokémon SV5" — Pokémon banner. **Homepage sections**: seed all 19 types including `social-feed`. Configs: `products` (title "Featured Collectibles", filterByBrand undefined, maxProducts 12), `auctions` (title "Live Auctions"), `pre-orders` (title "Reserve Before It Ships"), `categories` (showOnHomepage categories), `brands` (all 13), `blog-articles` (maxPosts 3), `newsletter`, `faq` (showOnHomepage FAQs), `stores` (featured stores), `events`, `reviews`, `whatsapp-community` (placeholder groupLink), `welcome`, `stats` (4 stat counters: Products, Sellers, Happy Buyers, Rating), `trust-indicators`, `features`, `banner`, `newsletter`, `social-feed` (**disabled**, platform instagram, handle "letitrip.in"). All section ids `section-` prefixed. |
| P21 | Reviews + orders seed | M | ✅ | Session 63 | Rewrote reviews-seed-data.ts (15 reviews, review- prefix, distributed across 5 stores, rating mix 5★×11/4★×2/3★×1/2★×1). Rewrote orders-seed-data.ts (10 orders, order- prefix, all statuses: DELIVERED×3/SHIPPED×2/PROCESSING×1/PENDING×1/CANCELLED×1/REFUNDED×1/RETURN_REQUESTED×1, prices in paise). 0 TS errors. | **Reviews** (`appkit/src/seed/reviews-seed-data.ts`): 15 reviews, `review-` prefixed. Distributed: 4 on store-cardgame-hub products, 3 on store-diecast-garage, 4 on store-tokyo-toys-india, 2 on store-letitrip-official, 2 on store-bladers-paradise. Mix: 3 × 5-star, 5 × 5-star, 4 × 4-star, 2 × 3-star, 1 × 2-star. Fields: rating, title, body (100–200 words), images (2–3 Unsplash product shots on 5 reviews), isVerifiedPurchase (true on 10), storeId, productId, buyerId, publishedAt (spread over 6 months). **Orders** (`appkit/src/seed/orders-seed-data.ts`): 10 orders in all states. `order-` prefixed. States: 1 pending, 2 processing, 2 shipped (with tracking number + carrier), 2 delivered, 1 cancelled (buyer-initiated, refund pending), 1 refunded (full refund processed), 1 disputed (under review). Each order: buyerId (from P15), storeId, items (1–3 products each), totalAmount, paymentMethod (razorpay/cod/upi), shippingAddress, createdAt (spread over 30 days). All statuses cover billing/payout/inventory edge cases. |
| P22 | Coupons + notifications seed | S | ✅ | Session 63 | Rewrote coupons-seed-data.ts (5 coupons: WELCOME10/POKEMON25/FREESHIP999/BLADER20/VIP2026, coupon- prefix, covers percentage/fixed/free_shipping types, partial-usage + exhausted states). Rewrote notifications-seed-data.ts (10 notifications, notif- prefix, uses NOTIFICATION_FIELDS constants, covers all 10 notification types, mixed read/unread). 0 TS errors. | **Coupons** (`appkit/src/seed/coupons-seed-data.ts`): 5 coupons, `coupon-` prefixed. (1) `coupon-welcome10` — 10% off, min order ₹500, max discount ₹200, first-order only, active. (2) `coupon-pokemon25` — ₹250 fixed off, scoped to `category-pokemon-cards` + `category-sealed-products`, min ₹1000, active. (3) `coupon-freeship999` — free shipping, min order ₹999, no max discount, active. (4) `coupon-blader20` — 20% off, scoped to `category-beyblade-tops`, max uses 50, 30 used so far (tests partial-usage UI). (5) `coupon-vip-exclusive` — 25% off, max uses 10 (limit reached → expired UI), tests exhausted coupon state. **Notifications** (`appkit/src/seed/notifications-seed-data.ts`): 10 notifications, all types (order-shipped, auction-outbid, preorder-update, new-review, payout-processed, wishlist-price-drop, auction-ending-soon, coupon-expiring, new-message, welcome). Mixed read/unread. Distributed across buyer and seller users. Each: userId, type, title, body, isRead, link, createdAt. |

---

## Tier HS — Homepage Sections CMS

> All tasks depend on each other in order: HS1 (schema) → HS2+HS3+HS5 (builders) → HS4 (Google Reviews). HS1 also feeds into CF1 (carousel-as-section fix). All image fields use `MediaUploadField`. No JSON config ever surfaces to users.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| HS1 | Extend section schemas + new types | M | ⏳ | | **Schema-only.** `appkit/src/features/homepage/schemas/firestore.ts`: (1) Add `SectionType` values `"custom-cards"` and `"google-reviews"`. (2) Add `CustomCardsSectionConfig`: `layout: "grid"\|"row"\|"masonry"`, `columns?: 1\|2\|3\|4`, `cards: Array<{ id, image?, imageAlt?, eyebrow?, title?, body?, buttons?, formEmbed?, backgroundColor?, textColor?, borderRadius?, shadowLevel? }>`, `autoScroll?, scrollIntervalMs?`. (3) Add `GoogleReviewsSectionConfig`: `placeId`, `maxReviews?, minRating?, layout?, showRating?, showDate?, linkToGoogleMaps?, googleMapsUrl?`. (4) Enhance all resource section configs (`ProductsSectionConfig`, `AuctionsSectionConfig`, `PreOrdersSectionConfig`, `StoresSectionConfig`, `EventsSectionConfig`) — add `sortBy?: "latest"\|"oldest"\|"priceLow"\|"priceHigh"\|"featured"\|"onSale"\|"popular"`, `filterByCategory?: string`, `maxCount?: 5\|10\|20`, `loop?: boolean`. (5) Add `googleMapsApiKey?` + `googlePlaceId?` to `SiteSettingsCredentials` in `appkit/src/features/admin/schemas/firestore.ts`. (6) Fix `"carousel"` missing from POST validation enum in `src/app/api/admin/sections/route.ts`. Rebuild appkit dist, 0 TS errors. |
| HS2 | Complete all 11 missing admin section builders | L | ⏳ | | **Depends on HS1.** `appkit/src/features/admin/components/AdminSectionsView.tsx` — build proper form UI for every type currently falling back to raw JSON editor. Zero JSON textareas exposed to users — if a type genuinely has no builder, show a "Builder coming soon" placeholder instead. Builders to add: **(1) welcome**: h1, subtitle, description (textarea), showCTA toggle, ctaText, ctaLink URL input. **(2) trust-indicators**: title + repeater (icon select, title, description); add/remove/reorder. **(3) categories**: title, maxCategories (4–12), autoScroll toggle, scrollInterval slider. **(4) brands**: title, subtitle, maxBrands, autoScroll toggle, scrollInterval. **(5) banner**: height select (sm/md/lg/xl), backgroundImage (`MediaUploadField`), backgroundColor (token picker), gradient (from+to token picker), content title/subtitle/description, buttons repeater (0–3: text/link/variant), clickable toggle + clickLink. **(6) features**: title + features text repeater (add/remove). **(7) reviews**: title, maxReviews select (5/10/20), itemsPerView select (1/2/3), autoScroll toggle, scrollInterval. **(8) whatsapp-community**: title, description (textarea), groupLink URL, memberCount number, benefits repeater (text inputs), buttonText, testimonial (optional). **(9) faq**: title, subtitle, showOnHomepage toggle, displayCount number, expandedByDefault toggle, linkToFullPage toggle, categories multi-select. **(10) blog-articles**: title, maxArticles (3/4/6 select), showReadTime/showAuthor/showThumbnails toggles. **(11) newsletter**: title, description, placeholder, buttonText, privacyText, privacyLink. All image/media fields use `MediaUploadField`. Save via existing `PATCH /api/admin/sections/[id]`. |
| HS3 | Enhance 7 existing resource builders with sort/filter/count | M | ⏳ | | **Depends on HS1.** `AdminSectionsView.tsx` — extend products, auctions, pre-orders, stores, events, reviews, social-feed builders: **(1) Resource builders** (products/auctions/pre-orders/stores/events): add Sort By dropdown (Latest/Oldest/Price Low→High/Price High→Low/Featured/On Sale/Popular), Filter by Brand (`DynamicSelect` → `/api/brands`), Filter by Category (`DynamicSelect` → `/api/categories`), Max Items radio (5/10/20), Loop toggle. **(2) reviews**: add source radio (Platform reviews / Google Business Reviews); if Google selected → show placeId input. **(3) social-feed**: add layout select (Grid/Masonry/Carousel), showCaption toggle, showStats toggle, postType select. Also fix New Section flow: type-selector modal shows grid of all 21 type cards with icon + description; selecting type opens editor drawer pre-set to that type. All save/delete use `useToast`. `UnsavedChangesModal` on dirty drawer close. |
| HS4 | Google Business Reviews integration | M | ⏳ | | **Depends on HS1+HS2.** **(A) API proxy** `src/app/api/social-feed/google-reviews/route.ts` (NEW): `GET ?placeId=X&maxReviews=5&minRating=4` → calls Google Places API v1 `/details` with `fields=reviews,rating,user_ratings_total`; filters by `minRating`; slices to `maxReviews`; response cached `revalidate: 3600`. Returns `{ reviews: Array<{ authorName, authorPhotoUrl, rating, text, time, profileUrl }>, aggregateRating, totalRatings }`. Empty if `googleMapsApiKey` not configured in site_settings (returns `{ error: "not-configured" }`). **(B) Component** (NEW `appkit/src/features/homepage/components/GoogleReviewsSection.tsx`): async RSC; reads `placeId` from config (falls back to `site_settings.googlePlaceId`); calls proxy route; renders review cards (avatar + name, star rating, date, review text, "Posted on Google" badge with Google logo icon); empty state if not configured. Grid or carousel layout per `cfg.layout`. **(C) Wire** into `MarketplaceHomepageView.tsx` `renderSection` switch as `case "google-reviews"`. **(D) Credentials**: expose `googleMapsApiKey` + `googlePlaceId` in VA8 ⑧Integrations (alongside TikTok/DeviantArt fields already there). |
| HS5 | Custom Cards section — component + builder | M | ⏳ | | **Depends on HS1+HS2.** **(A) Component** (NEW `appkit/src/features/homepage/components/CustomCardsSection.tsx`): renders `cards[]` per `layout` — Grid: CSS grid `columns` cols; Row: `HorizontalScroller` wrapper (existing component); Masonry: CSS columns. Each card: optional image (full-width top, via `/media/` proxy, `next/image`), eyebrow, title, body text, buttons row, optional form embed (`<iframe sandbox="allow-forms allow-scripts" loading="lazy">`). Card bg/text color from CSS token vars. `autoScroll` wraps in `SectionCarousel` (existing). **(B) Wire** into `MarketplaceHomepageView.tsx` `renderSection` switch as `case "custom-cards"`. **(C) Builder form** (already covered in HS2 as one of the new builders): layout select, columns select, card repeater — per card: `MediaUploadField` (image), imageAlt, eyebrow, title, body (textarea), buttons repeater (0–3: label/href/variant/target), formEmbed URL, backgroundColor token picker, textColor token picker, borderRadius select, shadowLevel select; drag-reorder cards; remove card. |

---

## Tier CF — Carousel & Feed

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| CF1 | Hero Carousel full redesign — schema + component + admin editor | XL | ⏳ | | **Supersedes A6+F3 + VA1.** **(A) Schema** (`appkit/src/features/homepage/schemas/firestore.ts` + `types/index.ts`): Add `background: { type: "image"\|"video"\|"color"\|"gradient"; url?; mobileUrl?; thumbnail?; color?; gradientFrom?; gradientTo?; gradientAngle?; dimOverlay?: { enabled, opacity } }` to `CarouselSlideDocument` (replaces `media`). Update `GridCard` → `CarouselCard` with `zone: 1\|2\|3\|4\|5\|6` (row 1 = zones 1–3, row 2 = zones 4–6), `mobileZone?: 2\|5`, `hover?: { effect: "scale"\|"color"\|"glow"\|"none"; scaleValue?; colorValue? }`, `content.eyebrow?`, `content.textAlign?`, `buttons[].href` + `"ghost"\|"link"` variants. Add `settings?: { autoplayDelayMs?; height?: "viewport"\|"tall"\|"medium" }`. Keep `media`/`mobileMedia`/`gridRow`/`gridCol` as optional backward-compat aliases — component falls back gracefully. **(B) Component** (`HeroCarousel.tsx`): Full-height support (`"viewport"` = `min-h-screen`, `"tall"` = `min-h-[80vh]`, `"medium"` = `min-h-[60vh]`). Unified background renderer supporting all 4 types. No blur/bleed effects. Zone → CSS grid mapping (`zone = (gridRow-1)*3 + gridCol` for legacy). Mobile: card with `mobileZone: 2` → row 1 full-width centered; `mobileZone: 5` → row 2 full-width centered. Configurable hover (replace hardcoded `hover:scale-105`). Per-slide autoplay delay. Autoplay pauses on hover (existing logic correct). **(C) Admin list** (`AdminCarouselView.tsx`): Add "New Slide" button → `/admin/carousel/new`. Each row: drag handle, thumbnail preview, title, active badge, "Edit"/"Delete" `RowActionMenu`. Drag-reorder batch-updates `order` field. **(D) Admin editor** (NEW `AdminCarouselEditorView.tsx`): 4 sections — (1) Slide Info: title, active, order, autoplay delay override, height select. (2) Background: 4-tab selector (Image/Video/Color/Gradient) with `MediaUploadField` for image+video, token picker for color, two-color + angle for gradient; dim overlay toggle + opacity slider. (3) Cards: 0–2 cards; **zone picker** = 6-cell visual 2×3 grid — if card 1 occupies row 1, row 1 cells greyed out for card 2; per card: mobile zone radio, background, eyebrow/title/subtitle/description, text color + align, buttons repeater (0–3, label/href/variant/target), hover effect + scale/color, padding. (4) Live Preview: read-only single-slide HeroCarousel render. Save/Cancel/Delete with `useToast`. **(E) API routes**: GET+POST `/api/admin/carousel`, GET+PUT+DELETE `/api/admin/carousel/[id]`, POST `/api/admin/carousel/reorder`. **(F) Seed update**: `carousel-slides-seed-data.ts` → update to new `background`/`zone` fields; keep `media`/`gridRow`/`gridCol` as legacy aliases in the first pass. Rebuild appkit dist. `npx tsc --noEmit` → 0 errors. Visual check: light + dark, mobile + desktop. |

---

## Tier 5 — Admin Core CRUD

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| A2 | Admin Categories CRUD — list + create + edit | S | ✅ | Part 59 | AdminCategoryEditorView in appkit; /admin/categories/new + [id]/edit pages; GET/POST/PUT/DELETE API routes; CATEGORY_BY_ID endpoint added; actionHref on list |
| A1 | Admin Products CRUD — 3-mode (standard/auction/preorder) | L | ⏳ | | LIST: `AdminProductsView` EXISTS. NEW: `AdminProductEditorView`. REUSE: `ProductForm.tsx` EXISTS in products/components as seller form — extend with admin-only fields (isPromoted, featured, sellerId); use `Tabs` for mode selector |
| A3 | Admin Coupons CRUD — complex conditional form | L | ⏳ | | LIST: `AdminCouponsView` EXISTS. NEW: `AdminCouponEditorView`. REUSE: `Select`, `Input`, `Toggle`, `Tabs`, `FormGrid`, `SideDrawer` from ui/components; conditional sections per coupon type |
| A4 | Admin Blog CRUD — with RichTextEditor | M | ⏳ | | LIST: `AdminBlogView` EXISTS. NEW: `AdminBlogEditorView`. REUSE: `RichTextEditor` (K3 already exists), `Input`, `TagInput`, `Toggle`, `SideDrawer` from ui/components |
| A5 | Admin FAQs CRUD — with rich text answer | S | ⏳ | | LIST: `AdminFaqsView` EXISTS. NEW: `AdminFaqEditorView`. REUSE: `RichTextEditor`, `Input`, `Select`, `Toggle`, `SideDrawer` |
| A6+F3 | Admin Carousel CRUD + reorder + live preview | S | 🚫 | | **Superseded by CF1** — full carousel redesign task covers all of this and more |
| N3 | Admin Stores full management (slug, verify, suspend) | M | ⏳ | | LIST: `AdminStoresView` EXISTS. ADD: action buttons (approve/reject/suspend/verify) via `RowActionMenu` (EXISTS in ui/components). API: PUT /api/admin/stores/[uid]. New fields: isVerified, suspensionReason |
| B1 | Admin Users role/ban management | M | ⏳ | | LIST: `AdminUsersView` EXISTS. ADD: role dropdown + ban toggle per row via `RowActionMenu`. REUSE: `RoleBadge`, `StatusBadge`, `SideDrawer` for detail view. API: PUT /api/admin/users/[uid] |
| B2 | Admin Orders status + refund UI | M | ⏳ | | LIST: `AdminOrdersView` EXISTS. ADD: status update + tracking via `SideDrawer` detail view. REUSE: `SideDrawer`, `Select`, `Input`, `BulkActionBar` (all EXIST). API: PUT /api/admin/orders/[id] |
| N2 | Admin Reviews full moderation (approve/reject/feature/reply) | M | ⏳ | | LIST: `AdminReviewsView` EXISTS. ADD: action buttons via `RowActionMenu`. REUSE: `ReviewModal`/`ViewReviewModal` EXISTS for full view. API: PUT /api/admin/reviews/[id] |

---

## Tier 6 — Admin Finance & CMS

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| M1 | Admin Analytics dashboard — charts + date range | M | ⏳ | | VIEW: `AdminAnalyticsView` EXISTS + `AdminRevenueChart`/`AdminOrdersChart`/`AdminTopProductsTable` all EXIST in appkit analytics/ — wire to real API; REUSE: `DashboardStatsCard`, `StatsGrid` |
| M3 | Admin Payouts processing + export CSV | M | ⏳ | | LIST: `AdminPayoutsView` EXISTS. ADD: mark-paid action via `RowActionMenu` + `BulkActionBar`. API: PUT /api/admin/payouts/[id]; GET /api/admin/payouts/export |
| F1 | Homepage Sections CMS | L | 🚫 | | **Superseded by HS1–HS5** — see Tier HS for full redesign |
| N1 | Site Settings full wiring | M | 🚫 | | **Superseded by VA8** which covers all 12 setting groups comprehensively. |
| F5 | Navigation CMS — dynamic nav items in Firestore | M | ⏳ | | VIEW: `AdminNavigationView` EXISTS. Complete CRUD using `SideDrawer` + `Input` + `Toggle` + drag-reorder. API: PUT/DELETE /api/admin/navigation/[id] |
| I1 | Deals/Featured inline toggles on admin products list | S | ⏳ | | REUSE: `RowActionMenu` (EXISTS) — add toggle actions to `AdminProductsView` rows via existing PUT /api/admin/products/[id] |
| B5 | Bids cancel/void | S | ⏳ | | LIST: `AdminBidsView` EXISTS. ADD: cancel via `RowActionMenu`. REUSE: `ConfirmDeleteModal` (EXISTS). API: PUT /api/admin/bids/[id] |
| B6 | Newsletter export CSV + unsubscribe | S | ⏳ | | LIST: `AdminNewsletterView` EXISTS. ADD: unsubscribe via `RowActionMenu` + export button in toolbar. API: DELETE /api/admin/newsletter/[id]; GET /api/admin/newsletter/export |
| B7 | Contact submissions mark-read + archive | S | ⏳ | | LIST: `AdminContactView` EXISTS. ADD: status actions via `RowActionMenu` + `SideDrawer` for detail. API: PUT /api/admin/contact-submissions/[id] |
| I3 | Sections seed reset button in admin sections toolbar | S | ⏳ | | Add a Button to `AdminSectionsView` toolbar. REUSE: `Button` + `ConfirmDeleteModal` (EXISTS). POST /api/demo/seed |

---

## Tier 7 — Store/Seller

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| O1 | Store slug management — create + change flow | S | ⏳ | | REUSE: `BecomeSellerView` EXISTS — add slug preview. In storefront: `Input` + availability check. API: PUT /api/store/profile with storeSlug |
| O2+C5 | Storefront full edit — bio, policies, branding, vacation mode | M | ⏳ | | Check if `SellerStorefrontView` is stub — complete with `RichTextEditor` (EXISTS) for bio/policies; `Toggle` for vacation mode; `AvatarDisplay` for logo/banner |
| C6 | Shipping config — method + prices + pickup address add | M | ⏳ | | REUSE: `StoreAddressSelectorCreate.tsx` EXISTS in stores/components for pickup address; `Radio`/`Toggle` for method selector. API: GET/PUT /api/store/shipping |
| C7 | Payout settings — UPI/bank config | M | ⏳ | | REUSE: `Radio` for UPI/bank toggle; `Input` for account fields. API: GET/PUT /api/store/payout-settings; return masked account number only |
| G1 | Product Templates CRUD | M | ⏳ | | NEW collection: product_templates. REUSE: `AdminListingScaffold` pattern for list; `SideDrawer` for create/edit; `Select`/`Input` for fields |
| G2 | Template apply in product/auction/preorder create forms | S | ⏳ | | REUSE: `DynamicSelect` (EXISTS) for template picker at top of `ProductForm`; add "Save as template" `Button` in form footer |
| C1 | Store Auctions create/edit | M | ⏳ | | REUSE: `ProductForm.tsx` EXISTS — check if it has isAuction mode; if yes, wire new pages with mode=auction pre-set; if no, extend ProductForm with auction field group behind isAuction tab |
| C2 | Store Pre-Orders create/edit | M | ⏳ | | REUSE: `ProductForm.tsx` EXISTS — same as C1; extend with isPreOrder field group; wire /store/pre-orders/new and /store/pre-orders/[id]/edit |
| C3 | Store Coupons edit page | S | ⏳ | | REUSE: existing seller coupon create form pattern — add /store/coupons/[id]/edit page pointing to same form component with couponId prop |
| C4 | Store Orders detail + status + tracking | M | ⏳ | | REUSE: `SideDrawer`, `Select`, `Input`, `StatusBadge` (all EXIST) for order detail drawer. API: PUT /api/store/orders/[id] for status+tracking |
| O4 | Store Analytics seller view | M | ⏳ | | VIEW: `SellerAnalyticsView` may exist — check; if stub, REUSE `AdminRevenueChart`/`AdminOrdersChart` with seller-scoped data. API: GET /api/store/analytics |
| O5 | Shiprocket auto-create on ship | M | ⏳ | | Server-side only: extend PUT /api/store/orders/[id] to call Shiprocket API when status=shipped + method=shiprocket; deferred if complex |

---

## Tier 8 — User Account

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| D1 | Wishlist page wiring | S | ⏳ | | REUSE: `WishlistView` EXISTS (exported from client.ts) — check /wishlist page.tsx exists; wire API. REUSE: `useGuestWishlist`/`useWishlistWithGuest` hooks EXIST |
| D2 | User Profile full edit | S | ⏳ | | REUSE: `ProfileView` EXISTS + `useProfile`/`useUpdateProfile` hooks EXIST — check which fields are wired; add missing publicProfile fields; `ImageCropModal` EXISTS for avatar |
| D3 | User Settings complete — password, email, privacy | M | ⏳ | | REUSE: `UserSettingsView` EXISTS — complete renderAppearance + add password change section; `Toggle` + `Input` for settings |
| D4 | Notifications view + mark read + delete | S | ⏳ | | REUSE: `UserNotificationsView` EXISTS + `NotificationBell` EXISTS — complete API wiring; `Tabs` (EXISTS) for tab filtering |
| I2 | Admin Payout processing | M | ⏳ | | Covered in M3 (Tier 6) |
| D5 | Messages — conversation view (deferred) | L | ⏳ | | REUSE: `MessagesView`, `ChatList`, `ChatWindow` all EXIST in account/components — wire Firebase RTDB; deferred to last |

---

## Tier Q — Query/Sieve via Firebase Function

> **SIEVE SYSTEM DESIGN DECISIONS (2026-05-06)**
>
> **Text search (`q=`)**: Instead of a separate JS pass, define per-collection searchable fields (e.g. `title`, `description`, `tags`). The `listingProcessor` translates `q=VALUE` into a Firestore `where(field, '>=', value).where(field, '<=', value+'')` range query on each searchable field, combined with the `f=` filters in one Firestore call. This avoids the JS in-memory pass entirely. Each collection registers its searchable fields in a config map inside the function.
>
> **Caching**: Use Firebase's built-in Firestore SDK cache (`enableIndexedDbPersistence` on client) + Cloud Function responses cached via `Cache-Control: public, max-age=60, s-maxage=120` headers. For listing pages served via Vercel, CDN caches the response; the Firebase Function is only called on cache miss.
>
> **Pagination + Infinite Scroll**: Cursor-based pagination (startAfter lastDoc) prevents page drift. Function returns an opaque `cursor` token alongside items. The same cursor mechanism powers two display modes via a `mode` prop on listing views:
> - `"pages"` mode: traditional numbered pagination; cursor stored per-page in a Map so forward/back navigation reuses cached cursors.
> - `"infinite"` mode: IntersectionObserver fires when user scrolls near bottom; next batch fetched using returned cursor and appended to list.
> **Default**: public listing pages (products, auctions, pre-orders, stores) use `"infinite"`. Admin/store data tables use `"pages"`.
>
> **Filter format**: Standard sievejs expressions — `fieldName==value`, `fieldName>=value`, etc. No shorthand aliases.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| Q1 | `listingProcessor` Firebase HTTPS Function | M | ⏳ | | File: `functions/src/callable/listing-processor.ts`. Input: `{ collection, q?, f?, s?, p?, ps?, cursor? }`. Text search via per-collection field config (range queries, no JS pass). Cursor-based pagination. Cache-Control header on response. Set `minInstances: 1`. Returns `{ items, total, page, pageSize, cursor, hasMore }`. |
| Q2 | Standardise listing API query param names | S | ⏳ | | All `/api/products`, `/api/pre-orders`, `/api/stores` routes accept short names: `f=` (filters), `s=` (sort), `p=` (page), `ps=` (pageSize), `q=` (search), `cursor=` (pagination cursor). Map internally. Keep old param names working for backwards compat. |
| Q3 | Delegate listing API routes to `listingProcessor` Firebase Function | M | ⏳ | | Replace direct Sieve calls in `/api/products/route.ts` and `/api/pre-orders/route.ts` with HTTP calls to `listingProcessor`. Use Firebase Admin SDK server-side. Vercel route becomes thin proxy. |
| Q4 | Update appkit listing views for new param names + cursor pagination | S | ⏳ | | `AuctionsListView`, `ProductsIndexPageView`, `PreOrdersListView`, `StoreProductsPageView`: read `f`, `s`, `p`, `ps`, `cursor` from `searchParams`. Store cursor Map for forward/back navigation. |
| Q5 | Firestore composite indexes for common filter+sort combos | S | ⏳ | | Add to `firestore.indexes.json`: `(category, price)`, `(brandSlug, createdAt)`, `(storeId, status)`, `(isAuction, auctionEndDate)`, `(isPromoted, createdAt)`, `(featured, createdAt)`. Deploy with `firebase deploy --only firestore:indexes`. |
| Q6 | Infinite scroll on public listing pages | M | ⏳ | | Add `useInfiniteScroll` hook in appkit: wraps `IntersectionObserver` on a sentinel `div` at list bottom; on intersect fetches next cursor batch and appends to items state. Wire into `ProductsIndexPageView`, `AuctionsListView`, `PreOrdersListView`, `StoreProductsPageView` as `mode="infinite"`. Show skeleton cards while loading next batch. "Back to top" button appears after 2 pages loaded. Admin/store tables keep `mode="pages"`. |

---

## Tier V — UX Completeness (All forms must be field-by-field, designed for non-technical users)

### V-A: Admin CRUD Editor Forms

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| VA1 | Admin Carousel create/edit form | S | 🚫 | | **Superseded by CF1** — full carousel redesign covers this |
| VA2 | Admin Products CRUD — 3-mode editor | L | ⏳ | | See A1. Ensure 3 tabs: Standard / Auction / Pre-Order. Admin-only fields: isPromoted toggle, featured toggle, sellerId override. REUSE: `ProductForm.tsx` as base. |
| VA3 | Admin Categories CRUD editor | S | ⏳ | | See A2. NEW `AdminCategoryEditorView`. Fields: name, slug (auto-generated), parentCategory (select), description, imageUrl, isActive toggle. |
| VA4 | Admin Blog CRUD editor | M | ⏳ | | See A4. NEW `AdminBlogEditorView`. Fields: title, slug (auto), content (RichTextEditor), excerpt, coverImage (MediaPicker), tags (TagInput), author, publishedAt (date), status (select), featured (toggle). |
| VA5 | Admin FAQs CRUD editor | S | ⏳ | | See A5. NEW `AdminFaqEditorView`. Fields: question, answer (RichTextEditor), category (select), order (number), isActive (toggle). |
| VA6 | Admin Coupons CRUD editor | L | ⏳ | | See A3. NEW `AdminCouponEditorView`. Fields vary by type: percent/fixed/freeShipping. Conditional sections: min order, max discount, product/category scope, usage limit, expiry date, per-user limit. |
| VA7 | Admin Navigation CMS editor | M | ⏳ | | See F5. Fields: label, href, icon (select), order, parentId (nested select), isVisible (toggle). Drag-reorder handle on list. |
| VA8 | Admin Site Settings — comprehensive multi-group form (supersedes N1) | L | ⏳ | | `AdminSiteView` EXISTS — complete it. All data stored in `site_settings/singleton`. Use grouped tabs/sections (no JSON blobs). Every sensitive key field masked on display, revealed on click. Groups: **①Branding** — site name, tagline, logo (MediaUploadField), favicon (MediaUploadField), maintenance mode toggle + message; **②Appearance** — primary/secondary/accent color pickers, dark mode default, font family select; **③Announcement** — text, enabled toggle, link URL, background color; **④SEO** — default meta title pattern, meta description, default og:image (MediaUploadField), robots noindex toggle, canonical base URL; **⑤Contact & Social** — support email, phone, physical address, support hours; Instagram/Twitter/Facebook/YouTube/WhatsApp/LinkedIn/Pinterest URLs; **⑥Watermark** — type toggle (text/image), text field (default site name), image upload (MediaUploadField), size 0–100% slider, opacity slider, live preview; **⑦Fees & Commissions** — platform commission % per sale, buyer convenience fee %, payment gateway fee % (Razorpay), seller payout hold period (days), minimum payout amount, auction listing fee, pre-order listing fee, featured slot fee, promoted slot fee; **⑧Integrations & Keys** — Razorpay Key ID + Secret (masked), Shiprocket API key + secret (masked), SendGrid/SMTP config (host, port, user, password masked, from-address), Google Analytics measurement ID, Facebook Pixel ID, GTM container ID, Algolia App ID + Search key (masked, future); **⑨Shipping Defaults** — free shipping threshold, COD enabled toggle, default carrier select, max delivery radius (km); **⑩Auction Config** — global minimum bid increment (fixed ₹ or %), auto-extend window (minutes before end), auction settlement grace period (hours); **⑪Platform Limits** — max products per store, max images per product, max video size (MB), max custom fields per product (default 50), max custom sections per product (default 3), order cancellation window (hours); **⑫Legal Policies** — Terms of Service (RichTextEditor), Privacy Policy, Refund Policy, Shipping Policy, Cookie Policy — these power the CMS-driven policy pages. Save each group independently (per-tab Save button). API: GET/PUT `/api/admin/site`. |
| VA9 | Admin Orders status + tracking update form | M | ⏳ | | See B2. SideDrawer on row click: status select, tracking number input, carrier select, internal note, refund amount. Action buttons: Approve, Reject, Refund. |
| VA10 | Admin Users role/ban management form | M | ⏳ | | See B1. Row action: role select (admin/seller/user), ban toggle + reason input. SideDrawer shows full user profile. API: PUT /api/admin/users/[uid]. |
| VA11 | Admin Reviews moderation actions | M | ⏳ | | See N2. Row actions: Approve, Reject, Feature, Reply (text field). REUSE: `ReviewModal` for full view. API: PUT /api/admin/reviews/[id]. |
| VA12 | Admin Stores management form | M | ⏳ | | See N3. Actions: approve/reject/suspend (+ reason field), verify badge toggle. SideDrawer with store detail. API: PUT /api/admin/stores/[uid]. |
| VA13 | Admin Payouts mark-paid + CSV export | M | ⏳ | | See M3. Row action: mark-paid (with reference number input). Export CSV button in toolbar. API: PUT /api/admin/payouts/[id], GET /api/admin/payouts/export. |
| VA14 | Admin Newsletter unsubscribe + CSV export | S | ⏳ | | See B6. Row action: Unsubscribe (with confirm dialog). Export CSV button. API: DELETE /api/admin/newsletter/[id], GET /api/admin/newsletter/export. |
| VA15 | Admin Contact submissions — mark-read + archive | S | ⏳ | | See B7. SideDrawer shows full message. Actions: Mark Read, Archive, Reply (mailto link). API: PUT /api/admin/contact-submissions/[id]. |
| VA16 | Admin Bids — per-auction list + cancel/void | S | ⏳ | | See B5. Improve `AdminBidsView`: filter by productId, show bidder name/amount/time. Row action: Cancel/Void (with confirm modal). API: PUT /api/admin/bids/[id]. |
| VA17 | Admin Feature Flags real UI | S | ⏳ | | NEW. `AdminFeatureFlagsView` — list of flags with enable/disable toggle + optional %-rollout number field. API: GET/PUT `/api/admin/feature-flags`. |
| VA18 | Admin Media Library — upload + delete | M | ⏳ | | See I4. `AdminMediaView` real UI: image grid, upload dropzone, delete with confirm, copy URL button. |
| VA19 | Admin Analytics charts — wired to real data | M | ⏳ | | See M1. Wire `AdminRevenueChart`, `AdminOrdersChart`, `AdminTopProductsTable` to `adminAnalytics` callable function. Add date range picker. |

### V-B: Store/Seller Forms

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| VB1 | Store Coupon create/edit form | S | ⏳ | | See C3. NEW `SellerCouponEditorView`. Fields: code, type (percent/fixed/freeShipping), value, minOrderAmount, maxUsage, expiryDate, productScope (multi-select). |
| VB2 | Store Order detail + status update form | M | ⏳ | | See C4. SideDrawer: order items, buyer address, payment summary. Status select + tracking number input. Actions: Mark Shipped, Mark Delivered. API: PUT /api/store/orders/[id]. |
| VB3 | Store Payout request / withdraw form | M | ⏳ | | NEW (not previously tracked). `SellerPayoutRequestView`. Fields: amount (up to available balance), note. Pre-fill from payout-settings (bank/UPI). API: POST `/api/store/payouts/request`. |
| VB4 | Store Storefront full edit | M | ⏳ | | See O2+C5. `SellerStorefrontView`: bio (RichTextEditor), store logo (MediaPicker), banner image (MediaPicker), vacation mode (toggle + message field), return policy (RichTextEditor). |
| VB5 | Store Shipping config form | M | ⏳ | | See C6. Method select (standard/express/pickup), price fields per method, free-shipping threshold toggle + amount, pickup address selector (StoreAddressSelectorCreate). |
| VB6 | Store Payout settings form | M | ⏳ | | See C7. Radio: UPI / Bank Transfer. UPI: VPA input. Bank: account name, account number (masked on display), IFSC, bank name. API: GET/PUT /api/store/payout-settings. |
| VB7 | Store Addresses CRUD | M | ⏳ | | See O3. List + add/edit/delete. Form: label, address lines, city, state, pincode, phone. Default billing/shipping toggles. |
| VB8 | Store Auction create/edit | M | ⏳ | | See C1. Extend `ProductForm` with auction tab: startingBid, reservePrice, bidIncrement, auctionStartDate, auctionEndDate. |
| VB9 | Store Pre-Order create/edit | M | ⏳ | | See C2. Extend `ProductForm` with pre-order tab: depositPercent, estimatedDeliveryDate, productionStatus select, maxQuantity. |
| VB10 | Store Analytics wired to real data | M | ⏳ | | See O4. Wire `SellerAnalyticsView` to `storeAnalytics` callable function. Revenue, orders, top products, customer count graphs. |

### V-C: User Account Forms & Flows

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| VC1 | User Order detail — full render (items, address, payment, tracking) | S | ⏳ | | `OrderDetailView` on `/user/orders/view/[id]` — wire renderItems, renderAddress, renderPayment, renderTracking render props. Currently shows empty shell. |
| VC2 | User Order invoice download | M | ⏳ | | NEW. "Download Invoice" button on user order detail page. Server route: GET `/api/orders/[id]/invoice`. Generate PDF via `@react-pdf/renderer` or server-side HTML→PDF. |
| VC3 | User Profile full edit | S | ⏳ | | See D2. Wire all fields: displayName, bio, avatar (ImageCropModal), publicProfile toggle, social links. |
| VC4 | User Settings — password, email, privacy tabs | M | ⏳ | | See D3. Tabs: Account (email change, password change), Privacy (data export, delete account), Appearance (dark mode, language). |
| VC5 | User Notifications — view + mark read + delete | S | ⏳ | | See D4. Wire `UserNotificationsView`. Tab filter (all/unread/orders/system). Mark all read button. Delete individual notifications. |
| VC6 | User Wishlist — fix broken wiring + stale validation | S | ⏳ | | Wishlist currently not working. Wire `WishlistView` with `useWishlistWithGuest` hook. Add stale validation on mount (see W2). Shows saved products with remove + add-to-cart actions. |
| VC7 | Message system — Firebase RTDB wiring | L | ⏳ | | See D5. Wire `MessagesView` → `ChatList` → `ChatWindow` to Firebase RTDB. Real-time listeners for messages. Send message action. Conversation list shows last message + unread count. |

### V-D: Public Pages & Navigation

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| VD1 | /support Help Centre page | S | ⏳ | | See E6. Aggregates: search bar linking to FAQs, Contact form link, Order tracking link, Policy page links. Fixes dead footer link. |
| VD2 | Footer dead link audit + fix | S | ⏳ | | See E7. Fix `/faq` → redirect to `/faqs`. Ensure `/support` exists. Audit all 20+ footer links in `LayoutShellClient.tsx`. |
| VD3 | generateMetadata for all detail pages (SL4) | S | ⏳ | | Canonical URL, title, description, `openGraph.url` for product/auction/preorder/store/category/brand/blog/event detail pages. |
| VD4 | Category + brand display on product cards + listing filters | M | ⏳ | | Product cards show category name (→ `/categories/[slug]`) and brand name (→ `/brands/[slug]`). Listing page sidebars show category tree + brand filters. |
| VD5 | Store profile on product detail pages (not user profile) | S | ⏳ | | See J11. Product detail seller section: store name, store logo, store rating, "Visit Store" button → `/stores/[storeSlug]`. No user profile. |
| VD6 | Brands listing page | S | ⏳ | | See F2. `/brands` page with `BrandsListView`. Grid of brand logos/cards. Clicking → `/brands/[slug]`. |
| VD7 | Fix brand name: "LetItRip" → "LetiTrip" throughout `messages/en.json` | S | ⏳ | | **Critical branding bug.** Every occurrence of `"LetItRip"` in `messages/en.json` must be replaced with `"LetiTrip"`. Affects: `about`, `terms`, `privacy`, `cookies`, `refundPolicy`, `shippingPolicy`, `sellerGuide`, `howAuctionsWork`, `howPreOrdersWork`, `howOffersWork`, `howOrdersWork`, `howCheckoutWorks`, `howPayoutsWork`, `howReviewsWork`, `becomeSeller`, `securityPage`, `sellersPage`, `help`, `contact`, and all other namespaces. Also fix `metaDescription` values that reference the wrong brand. Run global search for `LetItRip` and `letitripip.in` after to confirm 0 remaining. |
| VD8 | Rewrite `about` page content to be collectibles-specific | M | ⏳ | | `messages/en.json` → `about` namespace (25 keys). Currently: generic "democratise commerce" boilerplate with no mention of collectibles. Rewrite to be LetiTrip-specific: **mission** — "India's #1 marketplace for Pokémon TCG, Yu-Gi-Oh!, Hot Wheels, Beyblades, and anime collectibles"; **How It Works** section — buyer/seller/bidder descriptions specific to collectibles (graded cards, diecast, sealed products); **values** — trust/authenticity (anti-fake listings), community (fellow collectors), innovation; **milestones** — collectibles-themed launch story. All 25 keys must have content that matches the site's actual identity. |
| VD9 | Expand `becomeSeller` + `sellerGuide` content | M | ⏳ | | `becomeSeller` namespace currently has only 9 keys — the page is a stub. Expand to ~25 keys covering: **requirements** (verified ID, bank account, collectibles niche), **step-by-step onboarding** (create account → store profile → first listing → go live), **what sellers can list** (sealed products, graded cards, vintage diecast, action figures, Beyblades), **earnings calculator** example (₹1000 sale → ₹917.40 payout), **seller tiers** (basic/verified/power seller). `sellerGuide` already has 42 keys but rewrite to be collectibles-specific — mention Pokémon card grading, Hot Wheels photography tips, how to price sealed ETBs vs singles. |
| VD10 | Expand + localise legal policy pages | L | ⏳ | | `messages/en.json` legal namespaces — all policies are short generic boilerplate needing expansion for India + collectibles context: **(1) `terms`** — expand to ~15 sections: add India-specific clauses (IT Act 2000/2008, Consumer Protection Act 2019), collectibles-specific rules (no counterfeit Pokémon cards, no reproduction vintage Hot Wheels), auction binding bid rules, dispute resolution via Indian consumer courts, jurisdiction: Maharashtra. **(2) `privacy`** — add DPDP Act 2023 (India's data protection law) compliance section, data retention periods, right to erasure request process, children's data (no under-13 accounts). **(3) `cookies`** — categorise into essential/analytics/marketing; list specific cookies (Firebase, Razorpay, analytics); opt-out instructions. **(4) `refundPolicy`** — expand `sections[]` from empty to 8+ sections: sealed products (unopened → full refund within 7 days), opened/graded items (no return unless authenticity dispute), auction wins (no refund except seller non-delivery), pre-order cancellation window (before dispatch only), return shipping (buyer pays unless item not as described), refund timeline (5–7 business days Razorpay reversal). All dates updated to 2026. |
| VD11 | Fix all missing `next-intl` translation keys across all public pages | M | ⏳ | | **Every `MISSING_MESSAGE` error must be eliminated.** Root cause: `messages/en.json` keys were written with different names than the ones the appkit view components actually call via `t("key")`. This is NOT a content task — it is an exact key-name alignment fix. **Known missing keys (found by auditing component source vs en.json):** **(1) `howPayoutsWork` namespace** — component `HowPayoutsWorkView.tsx` calls: `infoCard1Title`, `infoCard1Text`, `infoCard2Title`, `infoCard2Text`, `infoCard3Title`, `infoCard3Text`, `infoCard4Title`, `infoCard4Text` (en.json has these under `bankSetupTitle`/`minimumTitle`/`commissionTitle`/`rejectionTitle` — add the `infoCard*` aliases pointing to the same content); `diagramStep1Desc`–`diagramStep5Desc` (en.json has `diagramS1Desc`–`diagramS5Desc` — add `diagramStep*` aliases); `ctaTitle`, `ctaText`, `ctaPayoutSettings`, `ctaFees` (all absent — add with appropriate content). **(2) `howPreOrdersWork` namespace** — component calls `diagramStep5Badge` (en.json has `diagramStatus5` — add alias). **Approach for all other pages:** run a grep for `MISSING_MESSAGE` in the dev server console after seeding, OR grep all `appkit/src/features/about/components/*.tsx` and `appkit/src/features/*/components/*View.tsx` for `t("key")` calls then cross-reference against `messages/en.json`. Add every missing key in the correct namespace. Never change the component — only add the missing keys to `messages/en.json`. After: zero `MISSING_MESSAGE` errors on any page. |

---

## Tier R — CRUD Table UX Standard *(applies to every table in the app)*

> **Rule**: Every CRUD table (admin, store, user) must satisfy R1. Apply this when building any table in Tier 5/6/7/8/V.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| R1 | CRUD table UX standard — row actions, tooltips, result feedback | M | ⏳ | | Every table row must have an action menu (edit, delete, status change, etc.) with **tooltips** on icon-only buttons. Every action (button click, form submit, status change) must produce a visible result: success → `showToast(message, "success")` + navigate to next screen OR re-fetch list; failure → `showToast(reason, "error")` explaining WHY it failed (not just "something went wrong"). Use `useToast()` from appkit consistently — **NOT** local state message divs (see X2). No silent failures anywhere. Wire into every VA/VB/VC table as each is implemented. |

---

## Tier W — Cart & Wishlist Integrity

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| W1 | Cart stale data validation on open | M | ⏳ | | On cart open/mount: batch-validate each cart item — check product still exists, store not suspended/deleted, product not deleted. Remove stale items silently OR show "X items were removed because they're no longer available" info toast. Applies to **both** authenticated cart (Firestore) and localStorage guest cart. Stale item = product doc missing OR store doc missing OR product status != active. |
| W2 | Wishlist stale data validation on open | M | ⏳ | | Same pattern as W1 for wishlist. On wishlist page mount + on wishlist popover open: validate each wishlisted product. Remove items that no longer exist or whose store has been removed. Show info toast if any removed. Applies to `useWishlistWithGuest` and authenticated wishlist. |
| W3 | Out-of-stock handling in cart | S | ⏳ | | Cart must NOT auto-remove out-of-stock items. Instead show them in a separate "Out of Stock" section below in-stock items. Each out-of-stock item: grayed out, "Out of Stock" badge, quantity locked. Add **"View Product"** link on every cart item (in-stock and out-of-stock) that opens the product detail page in a new tab — so users can check what they were buying. Checkout button disabled if cart has only out-of-stock items. |
| W4 | Cart item product link — all product types | S | ⏳ | | Every cart item (standard product, auction, pre-order) must have a clickable product name + "View" icon link that routes to the correct detail page (`/products/[slug]`, `/auctions/[id]`, `/pre-orders/[id]`). Opens in new tab (`target="_blank"`). Apply to both guest localStorage cart and authenticated cart view. |

---

## Tier X — Code Quality (TypeScript, Theming, Toast Standardisation)

> Apply X1–X4 as a sweep pass: no new features, just quality fixes. Must run `npx tsc --noEmit` → 0 errors after X1.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| X1 | Fix all TypeScript issues — async params + eliminate `as any` | M | ✅ | Session 60 | Verified 2026-05-06: `npx tsc --noEmit` passes in both `letitrip.in/` and `appkit/` with 0 errors. No action needed. |
| X2 | Toast standardisation — replace state messages with `useToast` | S | ⏳ | | Replace `setSaveMessage`/`setSuccessMsg`/`setErrorMsg` state patterns with `showToast(message, variant)` in all admin editor components: `AdminAdEditorView`, `AdminBrandEditorView`, `AdminCategoryEditorView`, `AdminFeatureFlagsView`, `AdminNavigationView`, `AdminSectionsView`. Toast auto-dismisses (5s). State message divs can be removed. |
| X3 | Dark mode + responsive fixes for admin CRUD forms | M | ⏳ | | **Missing dark mode**: `AdminBrandEditorView` (0 dark: classes), `AdminCategoryEditorView` (minimal). All form labels, inputs, textareas, sections must have `dark:` variants. **Missing responsive**: `AdminBrandEditorView`, `AdminCategoryEditorView` are full-width only — add `sm:grid-cols-2` / `md:grid-cols-3` form grids for wider screens. Apply same audit to every new VA/VB editor built in Tier V. |
| X4 | Responsive + themed CRUD forms — global checklist | S | ⏳ | | Every form built in Tier V (VA/VB/VC) must pass this checklist before marking ✅: (1) Works on mobile 375px, (2) Works on tablet 768px, (3) Works in dark mode, (4) All inputs use design system tokens (no hardcoded hex colors), (5) Focus rings visible, (6) Error states styled, (7) Loading/disabled states on submit button. Add this checklist to the HOW TO WORK section. |
| X5 | Replace skeleton loaders with `PageLoader` component + 15s timeout | M | ⏳ | | NEW: `PageLoader` component in appkit — shows a centred spinner with "Loading…" text (not a skeleton). 15-second client-side timeout: if the page hasn't finished loading data after 15s, replace spinner with "Something went wrong. Please refresh the page." + a Refresh button. The timeout is appropriate for first SSR load on slow connections. Update all 15 `loading.tsx` files under `src/app/[locale]/` to render `<PageLoader />` instead of inline skeletons. Keep the shared `Skeleton` component for within-page partial loading states (e.g. lazy-loaded tabs, infinite scroll batches). The `error.tsx` `ErrorView` handles hard errors; `PageLoader` timeout handles slow/stalled loads. |
| X6 | Media filename slug convention — enforce in all upload handlers | S | ⏳ | | All media uploaded through `MediaUploadField` must call `generateMediaFilename({ slug: resourceSlug, type, index })` so filenames include the resource slug (e.g. `product-hot-wheels-redline-main-0.jpg`). Audit every `onUpload` / `handleGalleryUpload` callback in every editor form — pass the current form's slug value. This ensures media paths are traceable to their resource and avoids name collisions. |
| X7a | Extend CSS token system — add missing semantic palette + social brand tokens | M | ⏳ | | **Audit found 759 hardcoded hex occurrences across 60 CSS files in `appkit/src/ui/components/`.** Root cause: token system lacks palette tokens beyond primary/secondary/surface/border/text/error/warning. Extend `globals.css` (or `appkit/src/ui/tokens.css`) to add: `--appkit-color-emerald-{50,100,200,300,400,500,600,700,800,900}`, `--appkit-color-amber-*`, `--appkit-color-rose-*`, `--appkit-color-sky-*`, `--appkit-color-purple-*`, `--appkit-color-teal-*`, `--appkit-color-green-*`, `--appkit-color-slate-*` (for dark chrome/dev tools), `--appkit-color-text-on-primary`. Social brand colors: `--appkit-color-instagram: #E1306C`, `--appkit-color-facebook: #1877F2`, `--appkit-color-tiktok: #010101`, `--appkit-color-deviantart: #05CC47`, `--appkit-color-whatsapp: #25D366`, `--appkit-color-youtube: #FF0000`. All palette tokens need dark-mode overrides in `.dark {}`. Must rebuild appkit dist after. |
| X7b | Replace all hardcoded color violations with token references | L | ⏳ | | Depends on X7a. **(1) CSS files (60 files, 759 hex occurrences)**: Replace all raw hex values in `appkit/src/ui/components/*.style.css` with `var(--appkit-color-*)` references. Priority order: `Badge.style.css` (25), `SideDrawer.style.css` (32), `ListingLayout.style.css` (34), `Card.style.css` (45), `Radio.style.css` (28), `Select.style.css` (26), `BulkActionBar.style.css` (19), then remaining 53 files. Remove hex fallback values from `var(--appkit-color-primary, #3e7708)` patterns once tokens are defined. **(2) TSX inline styles (13 files)**: `AppLayoutShell.tsx`, `ErrorBoundary.tsx`, `GlobalError.tsx`, `CharacterHotspot.tsx`, `CharacterHotspotForm.tsx`, `HeroBanner.tsx`, `NewsletterBanner.tsx`, `PromoGrid.tsx`, `BrandDetailPageView.tsx`, `CategoryTableColumns.tsx`, `AdminAnalyticsCharts.tsx`, `SellerRevenueChart.tsx`, `RichText.tsx` — move inline hex to CSS class using token vars. **(3) `src/components/dev/DevToolbar.tsx`**: Replace ~20 hardcoded slate/blue hex values with `var(--appkit-color-slate-*)` tokens. After: `npx tsc --noEmit` + visual check in light + dark mode. |
| X8a | Extend Tailwind config + appkit tokens for layout utilities | M | ⏳ | | **Audit found**: 60+ raw `@media (min-width: Xpx)` queries, 30 z-index integers, 106+ hardcoded px heights, 63 raw box-shadows, 18 grid `minmax(Xpx)` values. Extend `tailwind.config.ts` + `appkit/src/tokens/tokens.css`: **(1) Breakpoint aliases** — add `screens` extend with semantic names mirroring Tailwind defaults so CSS can use `@screen md {}` and eliminate raw `@media (min-width: 768px)`. **(2) Z-index CSS vars** — add `--appkit-z-base: 0`, `--appkit-z-raised: 10`, `--appkit-z-overlay: 20`, `--appkit-z-dropdown: 30`, `--appkit-z-navbar: 40`, `--appkit-z-sidebar: 50`, `--appkit-z-modal: 60`, `--appkit-z-toast: 70`, `--appkit-z-tooltip: 80`, `--appkit-z-below: -1`; wire into Tailwind `zIndex` extend so `z-modal` etc. work as classes. **(3) Component size tokens** — `--appkit-size-input-sm: 2rem`, `--appkit-size-input-md: 2.5rem` (40px), `--appkit-size-input-lg: 2.75rem` (44px), `--appkit-size-avatar-xs/sm/md/lg`; eliminates magic 44px/40px/36px. **(4) Grid column min tokens** — `--appkit-grid-min-card: 220px`, `--appkit-grid-min-card-sm: 160px`, `--appkit-grid-min-card-lg: 300px`, `--appkit-grid-min-card-xs: 130px` for `minmax()` expressions. **(5) Shadow CSS vars** — `--appkit-shadow-sm/md/lg/xl/glow` matching Tailwind config values. **(6) Typography CSS vars** — `--appkit-font-size-2xs: 0.625rem`, `--appkit-line-height-tight: 1`, `--appkit-line-height-normal: 1.5`, `--appkit-letter-spacing-tight: -0.02em`, `--appkit-letter-spacing-snug: -0.01em`. Rebuild appkit dist after. |
| X8b | Replace all hardcoded layout/utility violations with token references | L | ⏳ | | Depends on X8a. **(1) Media queries** — `appkit/src/ui/components/Layout.style.css` (60+ raw breakpoint queries): replace `@media (min-width: 768px)` with `@screen md {}`. **(2) Z-index** (30 violations across Modal, Drawer, Dropdown, BackgroundRenderer, HorizontalScroller): replace `z-index: 50` with `z-index: var(--appkit-z-modal)` etc. **(3) Heights/min-heights** (106+ in Button, Select, Input, Avatar, EmptyState): replace `min-height: 44px` with `min-height: var(--appkit-size-input-lg)` etc. **(4) Box-shadows** (63 violations across Modal, Card, Button, Checkbox): replace raw `0 20px 45px rgba(...)` with `box-shadow: var(--appkit-shadow-lg)` or `@apply shadow-lg`. **(5) Font sizes** (9 files with `font-size: 10px`): replace with `font-size: var(--appkit-font-size-2xs)` or `@apply text-2xs`. **(6) Grid minmax** (18 in Layout.style.css): replace with `var(--appkit-grid-min-card-*)`. **(7) Line-height/letter-spacing** (13 files): replace with `var(--appkit-line-height-*)` / `var(--appkit-letter-spacing-*)`. After: full responsive + dark-mode visual check. |

---

## Tier S — Social Media Feed Sections

> Show live social media posts (Instagram, Facebook, TikTok, DeviantArt) as homepage sections. Each post card opens the original post in a new tab. Credentials stored in site_settings — never exposed to the client.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| S1 | Social feed API route + shared fetcher | M | ⏳ | | `GET /api/social-feed?platform=X&handle=X&postType=X&count=X`. Shared fetcher in `appkit/src/features/homepage/lib/social-feed-fetcher.ts` (Instagram via Meta Graph API v19, Facebook via Meta Graph API v19, TikTok via TikTok for Developers v2, DeviantArt via client-credentials OAuth2 gallery). Normalised `SocialPost` shape: `{ id, platform, imageUrl, videoThumbnailUrl?, caption?, permalink, mediaType, stats: { likes?, views?, comments? }, publishedAt }`. Cache-Control: `public, max-age=300, s-maxage=600, stale-while-revalidate=60`. Credentials sourced from `siteSettingsRepository.getDecryptedCredentials()` — never exposed to client. |
| S2 | `social-feed` section type + homepage components | M | ⏳ | | `SocialFeedSectionConfig` schema (platform, handle, postType, count 4–12, layout grid/masonry/carousel, showCaption, showStats). `SocialFeedSection` async RSC: fetches directly via shared fetcher, renders grid/masonry/carousel. `SocialPostCard`: square thumbnail, platform badge (brand colour), video play overlay, carousel indicator, hover overlay with caption excerpt + like/view/comment counts, click → `target="_blank"` to post permalink. Registered in `MarketplaceHomepageView.tsx` switch + `DEFAULT_SECTION_ORDER` (order 12). |
| S3 | Admin sections builder for `social-feed` type | S | ⏳ | | Typed builder in `AdminSectionsView`: platform Select (instagram/facebook/tiktok/deviantart), handle Input, postType Select (all/images/videos/reels), count Input (1–12), layout Select (grid/masonry/carousel), showCaption Checkbox, showStats Checkbox. Wired into `SUPPORTED_TYPED_BUILDERS`, `buildSocialFeedConfig()`, `parseSocialFeedBuilder()`, edit-mode pre-fill, create-mode reset to defaults. |
| S4 | Site settings credentials — TikTok + DeviantArt fields | S | ⏳ | | Add to `SiteSettingsCredentials` and `SiteSettingsCredentialsMasked`: `tiktokClientKey`, `tiktokClientSecret`, `tiktokAccessToken`, `deviantartClientId`, `deviantartClientSecret`. Facebook + Instagram already use `metaPageAccessToken` + `metaPageId`. Expose all social credential fields in VA8 ⑧Integrations group with masked inputs + reveal-on-click. |
| S5 | Seed data — social-feed section demo entry | S | ⏳ | | Add a **disabled** sample `social-feed` section to homepage sections seed: `{ type: "social-feed", enabled: false, order: 12, config: { platform: "instagram", handle: "letitrip.in", postType: "all", count: 9, layout: "grid", showCaption: true, showStats: true } }`. Disabled by default so it does not fail in dev (no real credentials). Admin can enable it once credentials are set in VA8 Integrations. |

---

## Tier GP — Grouped Listings *(standard products + pre-orders only; no auctions)*

> A "group" is a parent product linked to 1–N child products (e.g., "Human Toy Complete Set" links to "head", "legs", "arms" as separate fully independent listings). Depth is exactly 1 — no grandchildren. Each member has its own title, slug, price, images, and checkout flow. The group thread is visible as a collapsible "Show group (N items)" panel on every member's detail page.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| GP1 | Grouped listings schema + `ShowGroupSection` component | M | ⏳ | | **Schema** (`appkit/src/features/products/schemas/firestore.ts`): add to `ProductDocument`: `groupId?: string` (= parent slug; same on parent + all children), `isGroupParent?: boolean`, `groupParentSlug?: string` (null on parent; parent's slug on children), `groupChildSlugs?: string[]` (parent only), `groupTitle?: string` (e.g. "Human Toy Complete Set"). Slug convention: parent = `product-human-toy`; children append part name (`product-human-toy-head`). **New component** `appkit/src/features/products/components/ShowGroupSection.tsx` — receives `groupId` + `currentSlug` + `isParent` props; renders nothing if `groupId` absent. Fetches all products/pre-orders with matching `groupId`. **Parent view**: collapsible panel "Parts in this group (N)" → horizontal card row of children (thumbnail, title, price, condition badge, link). **Child view**: collapsible panel "Part of: [groupTitle]" → parent card first (highlighted "Set" badge) then sibling cards. Both collapsed by default; current product highlighted. Standard products + pre-orders only. Wire into `ProductDetailPageView` `belowFold` + pre-order equivalent. `npx tsc --noEmit` → 0 errors. |
| GP2 | Edit-screen "Group settings" panel + inline child creation + link-existing | M | ⏳ | | Depends on GP1. Add collapsible **"Group settings"** section to `AdminProductEditorView` and seller `ProductForm.tsx`. Hidden on auction forms. **Three states**: *(1) Not in a group* — "Start a group" button → sets `isGroupParent: true`, `groupId: this.slug`. *(2) This is the group parent* — shows `groupTitle` input; lists children (thumbnail, title, price, condition, "Unlink" button in batch write); **"Add child listing"** button → modal with **two tabs**: **Tab 1 — Create new child**: minimal form with only the fields that differ (title, slug auto-suggested as `parent-slug-[part]`, price, short description, condition, images via MediaUploadField); all other fields (category, brand, storeId, tags, shippingInfo, returnPolicy) shown as read-only "Inherited from parent" badges; on save: creates new product doc with inherited fields + minimal changes; updates parent `groupChildSlugs` atomically; toast; "Need more control? Edit the full listing after saving" note. **Tab 2 — Link existing**: searchable `DynamicSelect` across products + pre-orders by title or slug; validates: candidate not already in a group, not an auction; on confirm: batch-writes `groupId`/`groupParentSlug` onto the linked listing + adds its slug to parent `groupChildSlugs`. Also: "Dissolve group" button (confirm required) → batch-clears all group fields on parent + all children. *(3) This is a child* — "Part of: [groupTitle]" read-only banner with link to parent edit page; "Leave group" button (confirm). All multi-doc writes are atomic batch. |

---

## Tier SC — Sub-listing Categories *(all listing types: products, auctions, pre-orders)*

> A "sub-listing category" is a named bucket (e.g., "Base Set 1 Charizard 108/120") that groups multiple independent listings of the same real-world collectible with different conditions, grades, prices, and sellers. Each listing is still its own full product/auction/pre-order — the sub-listing category is just the thread linking them. The public page shows all members. The detail page shows a collapsible horizontal scroller "More listings in: [name]".

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| SC1 | Sub-listing categories — schema + repository + API routes | M | ⏳ | | **New Firestore collection**: `sublisting_categories`. **`SublistingCategoryDocument`** type (new file `appkit/src/features/products/schemas/sublisting-categories.ts` or add to categories schema): `id: string` (= slug), `slug: string` (prefix: `sublisting-`), `name: string`, `itemCode?: string` (e.g. "108/120", "PSA-10"), `description?: string`, `coverImage?: string` (`/media/<slug>` proxy URL), `productCount: number` (maintained via batch write), `createdAt: Date`, `updatedAt: Date`, `createdBy: string`. **New field on `ProductDocument`** (and auction/pre-order if separate schemas): `sublistingCategoryId?: string` (a listing belongs to at most 1 sub-listing category). **`SublistingCategoriesRepository`** — same pattern as `CategoriesRepository`: `findById`, `findBySlug`, `list(model)` (Sieve pagination), `create`, `update`, `delete`, `getListingsByCategoryId(id, limit?)` (returns products + auctions + pre-orders with matching `sublistingCategoryId`, ordered by price, default limit 10). **API routes**: `GET /api/admin/sublisting-categories` (list + search), `POST /api/admin/sublisting-categories` (create), `GET /api/admin/sublisting-categories/[id]` (get one), `PUT /api/admin/sublisting-categories/[id]` (update), `DELETE /api/admin/sublisting-categories/[id]` (delete — unlinks all members first by batch-clearing their `sublistingCategoryId`), `GET /api/sublisting-categories/[slug]` (public — returns category + first 20 listings), `GET /api/sublisting-categories/[slug]/listings` (paginated public listings). Slug prefix `sublisting-` registered in Tier 0-pre table. After: `npx tsc --noEmit` → 0 errors. |
| SC2 | Admin CRUD for sub-listing categories | M | ⏳ | | Depends on SC1. **List view** `AdminSublistingCategoriesView` (`appkit/src/features/admin/components/`): columns — name, itemCode, productCount, createdAt, "Edit"/"Delete" `RowActionMenu`. Uses `AdminListingScaffold` (existing). Search + pagination via `useUrlTable`. **Editor view** `AdminSublistingCategoryEditorView`: form fields — name (text input, required), itemCode (text input, optional, e.g. card set code), description (textarea), coverImage (`MediaUploadField`). Save via `POST /api/admin/sublisting-categories` (create) or `PUT /api/admin/sublisting-categories/[id]` (update). Uses `SideDrawer` or dedicated page. Delete via `ConfirmDeleteModal`. **Routes**: `/admin/sublisting-categories` (list) + `/admin/sublisting-categories/new` + `/admin/sublisting-categories/[id]/edit`. Add to **admin navigation sidebar**. All actions use `useToast`. |
| SC3 | `sublistingCategoryId` field on all listing forms + `SublistingCarouselSection` component | M | ⏳ | | Depends on SC1. **Form field** (this is the ONLY way listings are linked to sub-listing categories — no central "assign products" UI): add "Sub-listing category" `DynamicSelect` to product/pre-order/auction create+edit forms, pulling from `/api/admin/sublisting-categories`. Works exactly like the existing category field — pick existing or create inline (small modal: name + itemCode inputs). Selecting None clears the field. **New component** `appkit/src/features/products/components/SublistingCarouselSection.tsx`: receives `sublistingCategoryId` prop; renders nothing if absent. Fetches first 10 listings with matching `sublistingCategoryId` from public API. Renders as **collapsible panel** "More listings like this: [category name]" — collapsed by default. Inside: `HorizontalScroller` with listing cards — current listing highlighted. "View all (N) →" link → `/sublisting-categories/[slug]`. Wire into `ProductDetailPageView` `belowFold`, `AuctionDetailView` `belowFold`, and pre-order equivalent. `npx tsc --noEmit` → 0 errors. |
| SC4 | Public sub-listing category page + routes | M | ⏳ | | Depends on SC1+SC3. **New page** `src/app/[locale]/sublisting-categories/[slug]/page.tsx` — server component: fetches category + paginated listings via public API. Renders: category header (name, itemCode, coverImage, description, `productCount` badge), then paginated listing grid reusing existing `ProductCard`/`AuctionCard`/`PreOrderCard`. Breadcrumb: Home → Sub-listing Categories → [Name]. `generateMetadata` for SEO (og:title, og:description, og:image via coverImage). Add `ROUTES.PUBLIC.SUBLISTING_CATEGORIES` + `ROUTES.PUBLIC.SUBLISTING_CATEGORY(slug)` to constants. No-listing empty state must render without crash. |

---

## Tier SR — Global Search Redesign

> Replace the tab-based `/search/[query]/tab/[tab]/sort/[sortKey]/page/[page]` flow with direct resource-type navigation. The search bar gets an inline resource type selector; submitting navigates to that resource's own public listing page with `?q=` pre-filled. Zero fuzzy search, zero third-party dependency — all suggestions come from existing Firestore data.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| SR1 | Search bar redesign — resource type dropdown + navigate-to-listing-page | M | ⏳ | | **New layout (single row):** `[--- search input ---] [--- resource type dropdown ---] [🔍 Search]` with suggestions below. **Resource type → route**: Products→`/products?q=X`, Auctions→`/auctions?q=X`, Pre-Orders→`/pre-orders?q=X`, Stores→`/stores?q=X`, Categories→`/categories?q=X`, Brands→`/brands?q=X`, Events→`/events?q=X`, Blog→`/blog?q=X`, FAQs→`/faqs?q=X`. Default: **Products**; persisted to `localStorage("letitrip_search_type")`. **On submit / Enter:** `router.push(ROUTES.PUBLIC[TYPE] + '?q=' + encodeURIComponent(query))` — removes the old `router.push('/{locale}/search')`. **Type selector**: use appkit `Select` component; on mobile collapses to icon-only with full labels in dropdown list. **Suggestions**: pass `selectedType` to `useNavSuggestions` hook so suggestions filter by type (categories when type=categories, blog titles when type=blog, etc.); clicking a suggestion still navigates to that item's detail page. Keep existing overlay open/close, keyboard navigation (arrow keys, Enter), Escape-to-close. File: `appkit/src/features/search/components/Search.tsx` + `useNavSuggestions.ts`. Rebuild appkit dist after. `npx tsc --noEmit` → 0 errors. |
| SR2 | Simplify / redirect the old `/search` page | S | ⏳ | | Depends on SR1. The complex `/search/[query]/tab/[tab]/sort/[sortKey]/page/[page]` URL structure is no longer the primary flow. **Convert** `src/app/[locale]/search/page.tsx` to a redirect handler: read `?q=` and `?type=` URL params → redirect to `ROUTES.PUBLIC[type]?q=X` (default type = products). The nested route segments (`[searchSlug]/tab/[tab]/sort/[sortKey]/page/[page]`) become permanent redirects to maintain backward compatibility for bookmarks. `SearchResultsClient.tsx` can be removed or kept as a minimal "no results" landing. Keep `/search?q=X&type=products` as a valid deep-link URL format (SR1's output). |
| SR3 | Verify all public listing pages accept and pre-fill `?q=` from URL | S | ⏳ | | Depends on SR1. For each of: `/products`, `/auctions`, `/pre-orders`, `/stores`, `/categories`, `/brands`, `/events`, `/blog`, `/faqs` — confirm: (a) the page's data fetch passes `searchParams.q` into the listing query/filter; (b) the search input in the listing toolbar is **pre-filled** with the `q` value on page load (not empty). If any listing page's toolbar search input is not pre-filled from URL, wire it — the toolbar input must read from `useUrlTable`'s `q` state which is initialised from the URL param. This is the contract that makes SR1 work: the search bar navigates here and the page must reflect the query. |

---

## Tier RC — Routes & Navigation Centralization

> All navigation configs (admin sidebar, store sidebar, user sidebar, footer links) moved to one file. All ROUTES constants audited and extended. Button-vs-link convention enforced across the codebase.

| # | Task | Complexity | Status | Part | Notes |
|---|------|-----------|--------|------|-------|
| RC1 | Centralize all navigation configs into `src/config/navigation.ts` | M | ⏳ | | **Create** `src/config/navigation.ts`. Move out of their respective layout.tsx files: `ADMIN_NAV_GROUPS` (from `src/app/[locale]/admin/layout.tsx` lines 15–73) → centralized; `STORE_NAV_GROUPS` (from `src/app/[locale]/store/layout.tsx` lines 14–47) → centralized; `USER_NAV_GROUPS` (from `src/app/[locale]/user/layout.tsx` lines 16–40) → centralized. Also move footer link columns out of `LayoutShellClient.tsx` into `FOOTER_LINK_GROUPS`. Rules: every `href` in every nav item must use `ROUTES.*` constants — no hardcoded strings. After move: layout files just `import { ADMIN_NAV_GROUPS } from "@/config/navigation"`. Also export `PUBLIC_QUICK_LINKS` for any static public nav shortcuts. All nav item `href` values that are currently hardcoded strings must be replaced with the correct `ROUTES.*` reference in this same task. |
| RC2 | Audit + extend `ROUTES` constants; replace all hardcoded route strings | M | ⏳ | | **Add missing routes** to `appkit/src/next/routing/route-map.ts`: `ROUTES.PUBLIC.SUBLISTING_CATEGORIES`, `ROUTES.PUBLIC.SUBLISTING_CATEGORY(slug)`, `ROUTES.PUBLIC.SEARCH(q, type?)` helper, and any other gaps found in the audit. **Audit**: grep all `*.tsx` and `*.ts` in `src/` for: hardcoded `href="/..."` strings, `router.push("/...")` strings with literal paths. Every match must be replaced with the corresponding `ROUTES.*` constant. After this task, `src/` must contain zero hardcoded route strings — all routes flow through `ROUTES`. Rebuild appkit dist if route-map.ts changed. `npx tsc --noEmit` → 0 errors. |
| RC3 | Button vs Link convention audit + fix violations | M | ⏳ | | **Convention** (already added to Non-Negotiable Rules): `<Link href={ROUTES.*}>` (Next.js) = navigation; `<TextLink href={ROUTES.*}>` (appkit) = inline text navigation; `<Button variant="...">` = action only, never navigates. Never `<Button onClick={() => router.push(...)}>`. For styled-button navigation: use `asChild` pattern (`<Button asChild><Link href={ROUTES.*}>Label</Link></Button>`). **Audit**: grep `src/` and `appkit/src/features/` for `onClick.*router\.push` patterns and `<a href=` raw anchors (without Next `<Link>`). Fix each violation. Common fix: remove `onClick`, add `asChild` + wrap with `<Link>`. After: `npx tsc --noEmit` → 0 errors + visually verify nav still works. |
