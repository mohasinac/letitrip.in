import { Meta } from "@storybook/blocks";

<Meta title="Introduction" />

# @letitrip/react-library

Welcome to the Letitrip React Library documentation!

## 🎯 Overview

This library contains reusable React components, utilities, and hooks extracted from the main Letitrip application.

## 📦 What's Included

### Utilities ✅

- **Formatters**: 20+ formatting functions for currency, dates, numbers, text
  - Indian numbering system (Lakh, Crore)
  - Phone, pincode, UPI formatting
  - File sizes, durations, order IDs
- **Validators**: Email, phone, pincode, URL validation
  - India-specific: GST, PAN, Aadhar, IFSC
  - Password strength checking
- **Date Utils**: Date manipulation with null-safety
  - ISO string conversion
  - Date input formatting
  - Validation helpers
- **Price Utils**: Price formatting with discount calculations
- **Sanitize**: XSS prevention and input sanitization

### Components ✅

- **Value Displays** (20 components):
  - DateDisplay, RelativeDate, DateRange
  - Price with discount badges
  - Status badges (Auction, Payment, Shipping, Stock)
  - Address, Currency, Email, Phone
  - Rating, OrderId, SKU, Quantity
  - TimeRemaining, TruncatedText, Weight

### Future Components

- **Form Components**: 21 accessible form inputs (Coming soon)
- **UI Components**: Button, Card, Modal, etc. (Coming soon)
- **Pickers**: DateTimePicker, State, Pincode (Coming soon)

### Future Hooks

- **useMediaQuery**: Responsive design (Coming soon)
- **useDebounce**: Delayed updates (Coming soon)
- **useLocalStorage**: Local storage sync (Coming soon)
- And more...

## 🚀 Getting Started

Browse the sidebar to explore components and utilities. Each story includes:

- Live examples
- Interactive controls
- Code snippets
- Accessibility information

## 📖 Usage Examples

### Utilities

```typescript
import {
  formatCompactCurrency,
  isValidEmail,
  safeToISOString,
  cn,
} from "@letitrip/react-library/utils";

// Format Indian currency
formatCompactCurrency(150000); // "₹1.5L"

// Validate email
isValidEmail("user@example.com"); // true

// Safe date conversion
safeToISOString(new Date()); // "2026-01-12T..."

// Merge Tailwind classes
cn("px-4 py-2", isActive && "bg-blue-500");
```

### Components

```typescript
import {
  DateDisplay,
  Price,
  StockStatus,
} from "@letitrip/react-library/components";

function ProductCard({ product }) {
  return (
    <div>
      <h3>{product.name}</h3>
      <Price amount={product.price} originalPrice={product.mrp} />
      <StockStatus stock={product.stock} />
      <DateDisplay date={product.createdAt} format="medium" />
    </div>
  );
}
```

## ♿ Accessibility

All components follow WCAG 2.1 AA guidelines and include:

- Proper ARIA labels
- Keyboard navigation
- Screen reader support
- Focus management

## 🎨 Theming

Components use Tailwind CSS and can be customized via theme configuration.

---

**Version**: 1.0.0  
**Status**: In Development  
**Last Updated**: January 12, 2026
