rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Reset baseline: deny all client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
